# 热血传奇 Go 重制版 — 客户端 & 服务端开发计划

## 现状评估

| 组件 | 状态 | 说明 |
|------|------|------|
| `internal/mapformat` | ✅ 完成 | .map 解析器，12/14/20字节单元格，列优先→行优先 |
| `internal/wil` | ✅ 完成 | .wil/.wix 加载器，标准+ILib格式，调色板→RGBA |
| `internal/log` | ✅ 完成 | 分级日志（TRACE→ERROR） |
| `cmd/mapviewer` | ✅ 完成 | OpenGL 3.3 + ImGui，三层渲染，小地图，相机 |
| `cmd/wilviewer` | ✅ 完成 | OpenGL 3.3 + ImGui，浏览/动画/导出 |
| `cmd/client` | ❌ 空桩 | `func main() {}` |
| `cmd/server` | ❌ 空桩 | `func main() {}` |

## 依赖关系图

```
Phase 1 (基础层 — 共享数据 & 协议)
  ├── Phase 2 (客户端窗口 & 场景框架)
  │     ├── Phase 3 (游戏场景 & 地图渲染)
  │     │     ├── Phase 4 (角色系统)
  │     │     │     └── Phase 5 (输入 & 移动)
  │     │     └── Phase 8 (光照 & 雾效)
  │     └── Phase 6 (客户端网络层)
  │           └── Phase 14 (集成：登录→游戏)
  └── Phase 7 (服务端核心)
        ├── Phase 9 (地图 & 世界管理)
        │     ├── Phase 10 (玩家逻辑)
        │     │     └── Phase 11 (怪物AI & 战斗)
        │     │           └── Phase 12 (魔法系统)
        │     └── Phase 13 (NPC & 脚本)
        └── Phase 15 (社交系统)
              └── Phase 16 (进阶系统)
                    └── Phase 17 (打磨 & 内容)
```

---

## Phase 1: 基础层 — 共享数据 & 协议

**目标**: 将 Delphi `Common/` 目录的所有类型和6Bit协议移植为可复用的Go包

**复杂度**: M | **预计**: 1-2周

### 任务

#### 1.1 `internal/protocol/edcode.go` — 6Bit 编解码
移植 `Common/EDcode.pas`：
- `Encode6BitBuf(src []byte) []byte` — 3字节→4字符，偏移0x3C
- `Decode6BitBuf(src []byte) []byte` — 反向
- `EncodeMessage(msg DefaultMessage) string` / `DecodeMessage(s string) DefaultMessage`
- `EncodeString(s string) string` / `DecodeString(s string) string`
- `EncodeBuffer(buf []byte) string` / `DecodeBuffer(s string, buf []byte)`
- 可选 XOR 掩码表 (`EncodeBitMasks[256]`, `DecodeBitMasks[256]`)

#### 1.2 `internal/protocol/message.go` — 消息类型
移植 `Common/Grobal2.pas` 消息定义：
- `DefaultMessage` 结构体: `Recog int32, Ident uint16, Param uint16, Tag uint16, Series uint16`（16字节，packed）
- `MakeDefaultMsg(ident uint16, recog int32, param, tag, series uint16) DefaultMessage`
- 所有 `CM_*` 常量（客户端→服务端，约50个ID）
- 所有 `SM_*` 常量（服务端→客户端，约100个ID）
- 消息帧格式: `'#' + code(1位数字) + 编码数据 + '!'`

#### 1.3 `internal/protocol/types.go` — 共享数据结构
移植 `Common/Grobal2.pas` 类型：
- 方向常量: `DR_UP(0)` ~ `DR_UPLEFT(7)`
- 网格常量: `UNITX=48, UNITY=32, LOGICALMAPUNIT=40`
- 装备槽常量: `U_DRESS(0)` ~ `U_BUJUK(9)`（10个槽位）
- `StdItem` 结构体（60字节）: Name[20], StdMode, Shape, Weight, AniCount, Looks, DuraMax, AC/MAC/DC/MC/SC, Need/NeedLevel, Price
- `UserItem` 结构体（24字节）: MakeIndex, wIndex, Dura, DuraMax, btValue[14]
- `ClientItem` 结构体: StdItem + MakeIndex + Dura + DuraMax
- `Ability` 结构体（50字节）: Level, AC, MAC, DC, MC, SC, HP, MP, MaxHP, MaxMP, Exp, MaxExp, Weight, MaxWeight 等
- `NakedAbility` 结构体: DC, MC, SC, AC, MAC, HP, MP, Hit, Speed
- `Magic` 结构体: MagicID, Name[12], EffectType, Effect, Spell, Power, TrainLevel[4], MaxTrain[4]
- `UserMagic` 结构体: MagicInfo, Level, MagIdx, TranPoint, Key
- `HumData` / `HumDataInfo` — 完整角色存档结构
- `MsgHeader` 结构体: dwCode(0xAA55AA55), nSocket, wGSocketIdx, wIdent, wUserListIndex, nLength
- `ChrMsg` 结构体: Ident, X, Y, Dir, State, Feature, Saying, Sound
- 毒类型、攻击模式、物品类型等常量

#### 1.4 `internal/protocol/types_test.go` — 编码测试
- 往返测试: `EncodeMessage` → `DecodeMessage`
- 往返测试: `EncodeBuffer` → `DecodeBuffer`（各种大小）
- 验证 `DefaultMessage` 结构体大小 == 16字节
- 验证 `StdItem` 结构体大小 == 60字节
- 验证 `UserItem` 结构体大小 == 24字节

### 交付物
`protocol` 包能编解码所有 MIR2 网络消息。通过基于已知 Delphi 编码字节序列的单元测试验证。

### 关键设计决策
- 使用 `encoding/binary` + `binary.LittleEndian` 进行结构体序列化（Delphi是小端序）
- `StdItem.Name` 使用 `[20]byte`（定长、null终止）保证线路兼容性
- 所有常量定义为类型化枚举

---

## Phase 2: 客户端 — 窗口 & 场景框架

**目标**: 创建客户端应用壳，包含GLFW窗口、OpenGL上下文、ImGui集成、场景状态机和WIL资源管理器

**复杂度**: M | **预计**: 1-2周

### 任务

#### 2.1 `internal/engine/window.go` — GLFW 窗口
- `Window` 结构体：封装GLFW窗口创建（OpenGL 3.3 Core Profile）
- 可配置大小（默认1024×768）、标题、全屏切换
- 输入回调注册（键盘、鼠标、滚轮）
- 主循环抽象: `Run(updateFn, renderFn)`
- 从 `cmd/mapviewer/main.go` 提取GLFW设置代码

#### 2.2 `internal/engine/gl.go` — OpenGL 状态
- `GLState` 结构体: 单元四边形VAO/VBO、白色纹理、着色器程序
- `UploadTexture(rgba *image.RGBA) uint32`
- `DeleteTexture(id uint32)`
- `DrawQuad(texID uint32, x, y, w, h float32, model, proj mgl32.Mat4)`
- `DrawQuadColor(...)` — 纯色绘制
- `OrthoProj(width, height float32) mgl32.Mat4`
- 从 mapviewer/wilviewer 提取并统一

#### 2.3 `internal/engine/shader.go` — 着色器程序
- `MainShader` — 纹理四边形着色器（pos+uv, model+proj, alpha discard）
- `ColorShader` — 纯色着色器（覆盖层）

#### 2.4 `internal/engine/resourcemanager.go` — WIL 资源缓存
- `ResourceManager` 结构体：持有所有已加载WIL文件
- 字段: `Tiles, SmTiles *wil.File`, `Objects [15]*wil.File`, `Hum, Hair, Weapon *wil.File`, `Mon [28]*wil.File`, `Npc *wil.File`, `Magic, Magic2 *wil.File`, `Items, StateItem, DnItems *wil.File`, `Prguse, Prguse2, Prguse3 *wil.File`, `ChrSel *wil.File`, `Mmap *wil.File`, `Effect, Dragon, Event, HumEffect, MagIcon *wil.File`
- `Load(dataDir string) error` — 从 `asset/client/Data/` 加载所有WIL文件
- 怪物文件延迟加载（Mon1~Mon28）
- 每个WIL文件的纹理缓存: `GetTexture(wilFile *wil.File, index int) uint32`
- LRU淘汰策略

#### 2.5 `internal/engine/scene.go` — 场景状态机
- `SceneType` 枚举: `SceneIntro, SceneLogin, SceneSelectChr, SceneLoginNotice, ScenePlayGame`
- `Scene` 接口: `Open()`, `Close()`, `Update(dt float64)`, `Render()`, `OnKey(key, action)`, `OnMouse(x, y, button, action)`, `OnScroll(x, y float64)`
- `SceneManager` 结构体: `CurrentScene Scene`, `ChangeScene(t SceneType)`
- 每个场景类型的桩实现（渲染占位背景）

#### 2.6 `cmd/client/main.go` — 客户端入口
- 解析CLI参数（数据目录、服务器地址）
- 创建窗口、GL状态、资源管理器
- 初始化场景管理器（`SceneIntro`）
- 主循环: `sceneManager.Update(dt)`, `sceneManager.Render()`, ImGui渲染
- 退出时清理

### 交付物
客户端窗口打开后显示ImGui调试覆盖层，可通过键盘快捷键切换占位场景。WIL资源加载成功（通过日志验证图像数量）。

---

## Phase 3: 客户端 — 游戏场景 & 地图渲染

**目标**: 使用三层瓦片系统渲染游戏世界，支持相机和坐标变换

**复杂度**: L | **预计**: 2-3周

### 任务

#### 3.1 `internal/engine/camera.go` — 2D 相机
- `Camera` 结构体: X, Y float64, Zoom float32, ViewportW/H int
- `ScreenToWorld(sx, sy) (wx, wy)` — 屏幕→世界坐标
- `WorldToTile(wx, wy) (tx, ty)` — 世界→瓦片坐标
- `TileToScreen(tx, ty) (sx, sy)` — 瓦片→屏幕坐标
- `ViewportTiles() (minX, minY, maxX, maxY)` — 可见瓦片范围
- `CenterOn(tx, ty)` — 居中相机
- `Pan(dx, dy)`, `ZoomAt(factor, cx, cy)`
- `ClampToBounds(mapW, mapH)` — 边界约束
- 从 `cmd/mapviewer/renderer/camera.go` 提取

#### 3.2 `cmd/client/scene_play.go` — 游戏场景（地图渲染）
移植 `PlayScn.pas` 和 `cmd/mapviewer/renderer/renderer.go`：
- `PlayScene` 结构体: mapData, camera, GL状态, resourceManager, actor列表, effect列表
- `DrawTileMap()`:
  - 背景层 (Tiles.wil): stride-2, 2×2块, 48×32瓦片
  - 中间层 (SmTiles.wil): 逐格, 透明
  - 前景层 (Objects.wil): 逐格, 动画帧循环, 门偏移, 混合模式（火焰/灯光用加法混合）
  - 区域系统: 根据格子的 `Area` 字段延迟加载 `Objects{N+1}.wil`
- 坐标变换: `ScreenXYfromMCXY(cx, cy) (sx, sy)`, `CXYfromMouseXY(mx, my) (cx, cy)`
- 视口裁剪: 只渲染屏幕可见瓦片（带边距）

#### 3.3 `cmd/client/scene_play.go` — 移动验证
- `CanWalk(tx, ty int) bool` — 碰撞检测
- `CanRun(fromX, fromY, toX, toY int) bool` — 路径检查
- `CanFly(fromX, fromY, toX, toY int) bool` — 远程攻击路径检查
- `GetDoor(tx, ty) *Door`, `IsDoorOpen(tx, ty) bool`, `OpenDoor(tx, ty)`

#### 3.4 `cmd/client/minimap.go` — 小地图
- 从地图数据生成碰撞纹理（绿色=可行走，灰色=不可通行）
- 渲染小地图覆盖层，带视口矩形
- 点击导航支持
- 从 `cmd/mapviewer/renderer/minimap.go` 提取

### 交付物
客户端渲染完整MIR2地图（三层瓦片），支持相机平移/缩放（WASD + 鼠标滚轮），显示小地图。渲染质量与现有mapviewer一致。

---

## Phase 4: 客户端 — 角色系统

**目标**: 实现可视化角色系统（玩家、怪物、NPC）及动画

**复杂度**: XL | **预计**: 3-4周

### 任务

#### 4.1 `cmd/client/actor.go` — 角色基类 & 动画模板
移植 `Actor.pas`：
- `ActionInfo` 结构体: Start, Frame, Skip, FTime, UseTick
- `HumanAction` — 14个动作（站立、行走、跑步、左右冲、战斗模式、攻击、重击、大击、烈火准备、施法、坐下、受击、死亡）
- `MonsterAction` — 7个动作（站立、行走、攻击、暴击、受击、死亡、尸体）
- `HumanActions` 模板 — 与Delphi `HA` 完全一致的帧偏移
- `MonsterActions` 模板 — `MA9` ~ `MA47`（39个模板）
- 帧计算: `CalcFrame(action ActionInfo, direction int, frameOffset int) int` → `start + direction*(frame+skip) + offset`
- `WORDER` 数组 — 武器覆盖层排序（男/女各600项）

#### 4.2 `cmd/client/actor.go` — Actor 基础结构体
- 身份: RecogID int32, UserName string, DescUserName string
- 位置: CurrX, CurrY int, Dir int, ShiftX, ShiftY float64（像素偏移，用于平滑移动）
- 外观: Sex, Race, Hair, Dress, Weapon, Job, Appearance uint16
- 状态: Death, Skeleton, WarMode, Grouped bool
- 属性: Abil Ability, Gold int32, HitSpeed int
- 渲染: BodySurface uint32（GL纹理）, BodyOffset int, HairOffset, WeaponOffset int
- 动画: StartFrame, EndFrame, CurrentFrame int, FrameTime, StartTime int64
- 魔法: CurMagic UseMagicInfo, UseMagic bool
- 消息队列: MsgList []ChrMsg（带互斥锁的线程安全）
- 动作: *MonsterAction（指向对应的MA模板）

#### 4.3 `cmd/client/actor_hum.go` — THumActor
移植 `Actor.pas` 中的 THumActor：
- 附加表面: HairSurface, WeaponSurface, HumWinSurface（翅膀）uint32
- `LoadSurface()` — 从 Hum.wil 加载身体, Hair.wil 加载发型, Weapon.wil 加载武器, HumEffect.wil 加载翅膀
- `DrawChr()` — 组合渲染: 身体 + 发型 + 武器 + 翅膀，按 WORDER 覆盖层顺序
- `FeatureChanged()` — 装备变更时重新加载表面
- 帧偏移: `HUMANFRAME = 600`（每性别每装备集）

#### 4.4 `cmd/client/actor_npc.go` — TNpcActor
- `LoadSurface()` — 从 Npc.wil 加载（每NPC 60帧）
- `DrawChr()` — 单表面渲染
- NPC特效表面

#### 4.5 `cmd/client/actor_monster.go` — TMonsterActor
- `LoadSurface()` — 根据外观从 Mon{N}.wil 加载
- `DrawChr()` — 使用正确MA模板的单表面渲染
- 帧数: `MONFRAME=280`, `EXPMONFRAME=360`, `SCULMONFRAME=440`
- Dragon 和 Effect 处理

#### 4.6 `cmd/client/actor_manager.go` — 角色生命周期
- `ActorManager` 结构体: actors map[int32]*Actor（RecogID → Actor）
- `FindActor(id int32) *Actor`
- `FindActorXY(x, y int) *Actor`
- `NewActor(msg DefaultMessage, ...) *Actor` — 从服务器数据创建角色
- `DeleteActor(id int32)`
- `ClearActors()`
- Y轴排序渲染: 按Y坐标排序以正确处理重叠

#### 4.7 `cmd/client/animation.go` — 动画播放器
- `AnimationState` 结构体: currentFrame, startFrame, endFrame, frameTime, lastTick
- `Update(now int64) bool` — 足够时间流逝时推进帧，帧变更返回true
- `SetAction(action ActionInfo, direction int)` — 配置动画
- `Reset()` — 重置到第一帧

### 交付物
角色在地图上正确渲染和动画。可通过调试命令在任意位置创建测试角色（人类、怪物、NPC）。装备覆盖层正确渲染。

---

## Phase 5: 客户端 — 输入 & 移动

**目标**: 处理玩家输入，实现移动、战斗和UI交互

**复杂度**: M | **预计**: 1-2周

### 任务

#### 5.1 `cmd/client/input.go` — 输入处理器
- `InputHandler` 结构体: 按键状态、鼠标状态、动作队列
- 鼠标处理:
  - 左键: 走到瓦片 / 攻击怪物 / 与NPC交互 / 拾取物品
  - 右键: 跑到瓦片
  - Shift+左键: 强制攻击
  - 中键拖拽: 平移相机
- 键盘处理:
  - F1-F8: 魔法快捷键
  - 1-6: 腰带物品使用
  - B/C/E/G/S/W: 打开面板（背包/角色/装备/行会/状态/魔法）
  - ESC: 关闭面板 / 取消
  - Enter: 聚焦聊天输入
  - 方向键: 相机平移（聊天未聚焦时）

#### 5.2 `cmd/client/action_queue.go` — 动作队列
- `ActionQueue` 结构体: 待执行动作、动作锁状态
- `QueueWalk(dir)`, `QueueRun(dir)`, `QueueHit(dir)`, `QueueSpell(magicID, x, y)`
- 动作锁定: 必须等待服务器响应后才能发送下一个动作（匹配Delphi `ActionLockTime`）
- `ProcessQueue()` — 未锁定时发送下一个动作
- `UnlockAction()` — 服务器确认动作时调用
- 超时处理: 3秒后自动解锁（防卡死）

#### 5.3 `cmd/client/targeting.go` — 目标选择
- `GetCharacter(tx, ty int) *Actor` — 查找瓦片上的角色
- `GetAttackFocusCharacter() *Actor` — 范围内最近敌方
- `GetDropItems(tx, ty int) []GroundItem` — 地面物品
- 目标跟踪: `TargetCret`, `FocusCret`, `MagicTarget` 全局变量

### 交付物
玩家可点击地图行走/跑步，点击测试角色进行目标选择，使用键盘快捷键。移动经过碰撞地图验证。动作锁定防止命令刷屏。

---

## Phase 6: 客户端 — 网络层

**目标**: 实现客户端TCP网络通信，使用MIR2协议

**复杂度**: L | **预计**: 2周

### 任务

#### 6.1 `internal/netclient/client.go` — TCP 客户端
- `Client` 结构体: conn net.Conn, 接收缓冲区, 发送缓冲区, 互斥锁
- `Connect(addr string) error` — 建立TCP连接
- `Disconnect()`
- `Send(msg DefaultMessage, extra string)` — 编码并发送: `'#' + code + encoded_msg + '!'`
- `SendRaw(data []byte)` — 发送预编码数据
- 读循环（goroutine）: 缓冲传入数据，提取消息帧，分发到处理器
- `OnMessage handler func(msg DefaultMessage, body string)` — 解码消息回调
- 指数退避重连逻辑

#### 6.2 `cmd/client/net_handler.go` — 客户端消息处理器
移植 `ClMain.pas` 的 `DecodeMessagePacket`：
- 登录流程处理器:
  - `SM_CERTIFICATION_SUCCESS` → 请求角色列表
  - `SM_QUERYCHR` → 填充角色选择
  - `SM_NEWCHR_SUCCESS/FAIL` → 角色创建结果
  - `SM_STARTPLAY` → 进入游戏
  - `SM_PASSWD_FAIL` → 显示错误
- 游戏处理器:
  - `SM_NEWMAP`, `SM_CHANGEMAP` → 加载地图
  - `SM_TURN`, `SM_WALK`, `SM_RUN` → 角色移动
  - `SM_HIT`, `SM_STRUCK`, `SM_DEATH` → 战斗视觉
  - `SM_HEAR`, `SM_WHISPER`, `SM_SYSMESSAGE` → 聊天
  - `SM_ADDITEM`, `SM_DELITEM`, `SM_BAGITEMS` → 背包
  - `SM_ADDMAGIC`, `SM_MAGIC_LVEXP` → 魔法列表
  - `SM_OBJECTMOVE` → 角色传送
  - `SM_SHOWEFFECT` → 视觉特效

#### 6.3 `cmd/client/net_send.go` — 客户端消息发送器
移植 `ClMain.pas`：
- `SendLogin(account, password string)`
- `SendNewChr(name string, job, sex int)`
- `SendDelChr(name string)`
- `SendSelChr(name string)`
- `SendSay(text string)`
- `SendWalk(x, y, dir int)`, `SendRun(x, y, dir int)`
- `SendHit(x, y, dir, hitMode int)`, `SendSpell(x, y, dir, magicID int)`
- `SendPickup(x, y int)`, `SendDropItem(itemIdx, count int)`
- `SendTakeOnItem(itemIdx, slot int)`, `SendTakeOffItem(slot int)`
- `SendEat(itemIdx int)`
- `SendMerchantDlgSelect(npcID int, option string)`
- `SendBuyItem(itemIdx, count int)`, `SendSellItem(itemIdx int)`
- `SendRepairItem(itemIdx int)`
- `SendDealTry(targetID int)`, `SendAddDealItem(itemIdx int)`, `SendDealEnd()`
- `SendCreateGroup(memberName string)`, `SendGuildDlg()`

### 交付物
客户端可连接测试服务器，发送登录凭据，接收并显示响应。网络层通过基于已知Delphi编码消息的往返编码测试。

---

## Phase 7: 服务端 — 核心基础设施

**目标**: 构建M2Server核心：网络引擎、前端引擎、用户引擎和主循环

**复杂度**: XL | **预计**: 3-4周

### 任务

#### 7.1 `internal/netserver/server.go` — TCP 服务器
- `TCPServer` 结构体: 监听器, 接受循环, 连接管理
- `Listen(addr string) error`
- `Accept()` — 接受新连接
- 每连接一个goroutine: 读循环, 缓冲管理, 消息帧
- 消息帧: 从网关连接提取 `MsgHeader`
- `OnConnect`, `OnDisconnect`, `OnMessage` 回调

#### 7.2 `cmd/server/runsocket.go` — RunSocket（网络引擎）
移植 `RunSock.pas`：
- `RunSocket` 结构体: 网关数组（最多20个网关）, 互斥锁
- `AddGate(conn net.Conn)` — 接受RunGate连接
- `ExecGateBuffers(gateIdx int, data []byte)` — 解析缓冲数据，提取MsgHeader帧
- `ExecGateMsg(gateIdx int, header MsgHeader, body []byte)` — 处理单条消息
- `DoClientCertification(gateIdx int, user *GateUser, socket int, msg string)` — 客户端认证握手
- `CloseUser(gateIdx, socket int)` — 断开用户
- `SetGateUserList(gateIdx, socket int, player *PlayObject)` — 关联socket和玩家
- 网关用户管理: `GateUser` 结构体（socket, player引用, 缓冲区）

#### 7.3 `cmd/server/frnengine.go` — 前端引擎（DB I/O）
移植 `FrnEngn.pas`：
- `FrontEngine` 结构体: 加载队列, 保存队列, 金币变更队列, 互斥锁, done通道
- `Start()` — 启动goroutine进行DB I/O
- `Stop()` — 优雅关闭
- `AddToLoadRcdList(account, chrName, ipAddr string, ...)` — 排队角色加载
- `AddToSaveRcdList(saveRcd *SaveRcd)` — 排队角色保存
- `ProcessGameData()` — 处理保存/加载队列
- `LoadHumFromDB()` — 加载角色记录，为UserEngine创建UserOpenInfo
- `SaveHumRcdToDB()` — 持久化角色到存储
- 使用 SQLite 存储账号、角色、行会数据

#### 7.4 `cmd/server/usrengine.go` — 用户引擎（世界核心）
移植 `UsrEngn.pas`：
- `UserEngine` 结构体:
  - `PlayObjectList map[string]*PlayObject` — 在线玩家（name→player）
  - `PlayObjectFreeList []*PlayObject` — 断连玩家待清理
  - `LoadPlayList []*UserOpenInfo` — 正在加载的玩家
  - `StdItemList []*StdItemDef` — 物品模板列表
  - `MonsterList []*MonsterInfo` — 怪物定义
  - `MonGenList []*MonGenInfo` — 怪物刷新点
  - `MagicList []*MagicDef` — 魔法定义
  - `MerchantList []*Merchant` — NPC商人
  - `QuestNPCList []*QuestNPC` — 任务NPC
- 处理方法（每tick调用）:
  - `ProcessHumans()` — 处理所有玩家动作
  - `ProcessMonsters()` — 处理所有怪物AI
  - `ProcessMerchants()` — 商人NPC逻辑
  - `ProcessNpcs()` — 任务NPC逻辑
  - `ProcessEvents()` — 地图事件
  - `RegenMonsters()` — 重生死亡怪物
- 查询方法:
  - `GetStdItem(idx int) *StdItemDef`
  - `GetPlayObject(name string) *PlayObject`
  - `FindMagic(id int) *MagicDef`
  - `ProcessUserMessage(player *PlayObject, msg DefaultMessage, body string)` — 路由客户端消息

#### 7.5 `cmd/server/main.go` — 服务端入口
- 解析CLI参数（配置文件路径、端口）
- 初始化: MapManager, UserEngine, FrontEngine, RunSocket, MagicManager
- 从 `!setup.txt` 加载配置
- 加载物品定义、怪物定义、魔法定义
- 加载地图
- 启动FrontEngine goroutine
- 启动RunSocket监听器
- 主tick循环: 100ms间隔（10 ticks/秒，匹配Delphi）
  - `UserEngine.ProcessHumans()`
  - `UserEngine.ProcessMonsters()`
  - `UserEngine.ProcessMerchants()`
  - `UserEngine.ProcessNpcs()`
  - `UserEngine.ProcessEvents()`
  - `UserEngine.RegenMonsters()`
- SIGINT/SIGTERM 优雅关闭

### 交付物
服务端启动，监听端口7000，接受客户端连接，处理认证和游戏消息，以10 ticks/秒运行主游戏循环。可记录接收到的消息。

---

## Phase 8: 客户端 — 光照 & 雾效

**目标**: 实现Delphi中的6级光照系统

**复杂度**: M | **预计**: 1周

### 任务

#### 8.1 `cmd/client/lighting.go` — 光照系统
移植 `PlayScn.pas`：
- 从 `Data/lig0a~f.dat` 加载6个光罩文件:
  - `LightMask0` (3×3), `LightMask1` (5×5), `LightMask2` (9×9), `LightMask3` (11×11), `LightMask4` (15×15), `LightMask5` (17×17)
- `LightMap [30][26]LightMapInfo` — 每格: ShiftX, ShiftY, 光照等级, 亮度
- `FogScreen [800][445]byte` — 每像素雾浓度
- `ClearLightMap()` — 重置所有格子
- `AddLight(x, y, level int)` — 在位置应用光罩
- `UpdateBright()` — 从光图计算亮度
- `ApplyLightMap()` — 将亮度应用到雾屏
- `DrawLightEffect()` — 渲染半透明黑色纹理作为雾覆盖层

#### 8.2 `cmd/client/scene_play.go` — 光照集成
- 地图数据中的格子光照等级（`btLight` 字段）
- 玩家光照等级（装备相关）
- 动态光源: 火焰、法术、特效
- 黑暗地图（地图标志: `boDarkness`）
- 昼夜循环支持

### 交付物
带黑暗标志的地图正确渲染光照效果。光源（火把、法术）照亮周围区域。使用预计算光罩实现平滑光照衰减。

---

## Phase 9: 服务端 — 地图 & 世界管理

**目标**: 服务端地图加载、空间查询和对象放置

**复杂度**: L | **预计**: 2周

### 任务

#### 9.1 `cmd/server/envir.go` — 地图环境
移植 `Envir.pas`：
- `MapCellInfo` 结构体: Flag byte, ObjList []OSObject
- `OSObject` 结构体: Type byte, CellObj interface{}, AddTime int64
- 对象类型常量: `OS_EVENTOBJECT=1, OS_MOVINGOBJECT=2, OS_ITEMOBJECT=3, OS_GATEOBJECT=4, OS_MAPEVENT=5, OS_DOOR=6`
- `Environment` 结构体:
  - Header mapformat.Header
  - MapCellArray []MapCellInfo — 展平网格（列优先: `index = x*Height + y`）
  - Flag MapFlag — safe, fight, darkness 等
  - DoorList []Door
- `LoadMapData(filename string) error` — 通过 `internal/mapformat` 解析 .map 文件
- `AddToMap(x, y int, objType int, obj interface{}) bool` — 在格子放置对象
- `DeleteFromMap(x, y int, objType int, obj interface{}) bool` — 从格子移除
- `MoveToMovingObject(cx, cy int, obj *BaseObject, nx, ny int, flag bool) bool` — 移动实体
- `CanWalk(x, y int) bool`, `CanWalkEx`, `CanFly`
- `GetMovingObject(x, y int) *BaseObject` — 查询位置上的实体
- `GetRangeBaseObject(x, y, radius int, flag bool, list []*BaseObject)` — 区域查询
- `GetItem(x, y int) *GroundItem` — 地面物品

#### 9.2 `cmd/server/mapmanager.go` — 地图管理器
移植 `Envir.pas` TMapManager：
- `MapManager` 结构体: maps map[string]*Environment
- `AddMapInfo(name, desc string, serverNum int, flags MapFlag, questNPC string)` — 注册地图
- `AddMapRoute(srcMap string, srcX, srcY int, dstMap string, dstX, dstY int)` — 传送链接
- `FindMap(name string) *Environment`
- 启动时从 `asset/server/Map/` 加载所有地图
- 解析 `MapInfo.txt` 获取地图元数据和标志

#### 9.3 `cmd/server/doors.go` — 门系统
- `Door` 结构体: ID, Position, State (open/closed), OpenTick
- `ProcessMapDoors()` — 超时后自动关门
- 门开关消息广播

### 交付物
服务端从 `asset/server/Map/` 加载所有地图，支持空间查询（查找位置上的实体、区域搜索），处理门状态转换。

---

## Phase 10: 服务端 — 玩家逻辑

**目标**: 实现玩家连接生命周期、属性、装备和背包

**复杂度**: XL | **预计**: 3-4周

### 任务

#### 10.1 `cmd/server/baseobject.go` — TBaseObject
移植 `ObjBase.pas`（26821行 — 先实现核心）：
- 身份: MapName, CharName, CurrX/Y, Direction, Gender, Hair, Job
- 属性: Abil Ability, WAbil Ability（工作值）, AddAbil AddAbility
- 战斗: HitPoint, HitPlus, HitDouble, HitSpeed, Luck
- 状态: StatusTimeArr [12]int16, StatusArrTick [12]int64, StatusArrValue [6]int16
- 位置: PEnvir *Environment, HomeMap, HomeX/Y
- 社交: GroupOwner, GroupMembers, MyGuild, GuildRankNo
- 召唤: Master *BaseObject, SlaveList, SlaveExpLevel
- 装备: UseItems [13]*UserItem, ItemList []*UserItem（背包）, StorageList []*UserItem
- 魔法: MagicList []*UserMagic, 技能引用（OneSword, PowerHit, Ergum, Banwol, FireSword, Crs, TwinHit）
- 可见性: VisibleActors, VisibleItems, VisibleEvents, VisibleHumanList
- 消息: MsgList []SendMessage（线程安全队列）
- 战斗标志: PowerHit, UseThrusting, UseHalfMoon, FireHitSkill, CrsHitkill
- 特殊物品: Teleport, Paralysis, Revival, MagicShield, FlameRing
- PK: PKPoint, PKFlag, PKTick
- 核心方法:
  - `SendMsg`, `SendFirstMsg`, `SendDelayMsg`, `SendRefMsg` — 消息传递
  - `WalkTo(dir int) bool` — 移动
  - `TurnTo(dir int)` — 转向
  - `Attack(hitMode int, target *BaseObject)` — 战斗结算
  - `GetHitStruckDamage(attacker *BaseObject, damage int) int`
  - `GetMagStruckDamage(attacker *BaseObject, damage int) int`
  - `MakeSlave(name string, count, level int) []*BaseObject`
  - `MakePoison(poisonType, level, duration int)`
  - `EnterAnotherMap(envir *Environment, x, y int) bool` — 地图切换
  - `FeatureChanged()` — 广播外观变更
  - `GetFeature() int32` — 序列化外观
  - `SpaceMove(mapName string, x, y int)` — 传送
  - `TrainSkill(magic *UserMagic, count int)` — 技能训练
  - `CheckMagicLevelup(magic *UserMagic)` — 升级检查
  - `RecalcLevelAbilities()` — 升级时重算属性
  - `DropItemDown(item *UserItem, scatter int)` — 地面掉落物品
  - `DropGoldDown(gold int, scatter int)` — 地面掉落金币

#### 10.2 `cmd/server/playobject.go` — TPlayObject
移植 `ObjBase.pas`（TPlayObject部分）：
- 扩展TBaseObject
- 连接: GateIdx, Socket, UserOpenInfo
- 会话: AccountName, SessionID, LoginTick
- 游戏状态: MapMove, Loading, ReadyToRun
- `Operate()` — 每tick主处理
- `ClientMsg(msg DefaultMessage, body string) bool` — 处理客户端消息
- 消息处理器:
  - Walk/Run/Turn → 验证并执行
  - Hit/Spell → 验证并执行
  - Pickup/Drop/Use item → 背包管理
  - TakeOn/TakeOff → 装备管理
  - Say → 聊天广播
  - Merchant交互 → NPC对话

#### 10.3 `cmd/server/inventory.go` — 背包系统
- `AddItem(item *UserItem) bool` — 添加到背包（46格）
- `DeleteItem(item *UserItem) bool` — 从背包移除
- `FindItem(idx int) *UserItem` — 按索引查找
- `FindItemByName(name string) *UserItem` — 按名称查找
- `ItemCount() int` — 当前背包数量
- `TakeOnEquipment(item *UserItem, slot int) bool` — 穿戴装备
- `TakeOffEquipment(slot int) *UserItem` — 脱下装备
- `UseItem(item *UserItem) bool` — 使用/消耗物品
- 重量计算: `CalcBagWeight()`, `CalcWearWeight()`, `CalcHandWeight()`

#### 10.4 `cmd/server/itemdef.go` — 物品定义
移植 `ItmUnit.pas`：
- `StdItemDef` 结构体 — 匹配TStdItem的物品模板
- `LoadStdItems(filename string) error` — 从 `StdItems.txt` 加载
- `GetStandardItem(idx int) StdItem` — 复制基础属性
- `GetItemAddValue(userItem *UserItem, stdItem *StdItem)` — 应用每实例升级
- `RandomUpgradeItem(userItem *UserItem)` — 掉落时随机属性加成
- `ApplyItemParameters(userItem *UserItem, addAbil *AddAbility)` — 应用到角色

#### 10.5 `cmd/server/config.go` — 服务端配置
移植 `M2Share.pas` g_Config：
- `ServerConfig` 结构体 — 所有服务端设置
- `LoadConfig(filename string) error` — 解析 `server_config/server.jsonc`（主配置）
- `LoadStringConfig(filename string)` — 解析 `server_config/strings.jsonc`（提示文本）
- `LoadExpConfig(filename string)` — 解析 `server_config/exp_table.jsonc`（经验表）
- `LoadBaseAbil(filename string)` — 解析 `server_config/base_abil.jsonc`（基础属性）

### 交付物
服务端可从文件加载角色数据，创建PlayObject实例，处理基本客户端消息（登录、行走、转身），管理背包/装备。可通过直接消息注入测试。

---

## Phase 11: 服务端 — 怪物AI & 战斗

**目标**: 实现怪物系统和战斗结算

**复杂度**: XL | **预计**: 3-4周

### 任务

#### 11.1 `cmd/server/monster.go` — 怪物基类
移植 `ObjMon.pas` 和 `ObjBase.pas` 怪物部分：
- `Monster` 结构体扩展BaseObject
- `MonsterInfo` 结构体: Name, Race, RaceImg, Appr, Level, HP, MP, AC, MAC, DC, MC, SC, Speed, Hit
- `LoadMonsters(filename string) error` — 从 `MonGen.txt` / `Monster.DB` 加载
- `MonGenInfo` 结构体: MapName, X, Y, MonsterName, Range, Count, Interval
- `Operate()` — 每tick AI处理
- `AttackTarget()` — 基础近战攻击
- `FindTarget()` — 获取最近敌方
- `GotoTarget()` — 向目标移动
- `TakeItem()` — 拾取物品（贪婪怪物）

#### 11.2 `cmd/server/monster_types.go` — 怪物变体
移植20+个不同行为的怪物类：
- `Monster` — 基础行走怪物
- `FastMonster` — 更快攻击速度
- `RangedMonster` — 远程攻击（投射物）
- `StationaryMonster` — 不移动（树、雕像）
- `TeleportMonster` — 随机传送
- `ExplosionMonster` — 攻击时自爆
- `TameableMonster` — 可被道士驯服
- `UndeadMonster` — 受亡灵克制影响
- `SummonMonster` — 玩家召唤，跟随主人
- `GuardMonster` — 城镇守卫，攻击PK玩家
- `BossMonster` — 特殊能力，范围攻击

#### 11.3 `cmd/server/combat.go` — 战斗系统
从 `ObjBase.pas` 移植伤害计算：
- `HitDamage(attacker, target *BaseObject, hitMode int) int` — 物理伤害
- `MagDamage(attacker, target *BaseObject, magicID int) int` — 魔法伤害
- 伤害公式: `DC - target.AC` + 随机方差
- 暴击: `PowerHit` 双倍伤害
- 连击: `HitDouble` 额外攻击几率
- PK系统: `PKPoint` 累积, `PKFlag`（红名）, `PKTick`（衰减）
- 死亡处理: 掉落物品、掉落金币、在复活点重生
- 攻击模式: `HAM_ALL`, `HAM_PEACE`, `HAM_GROUP`, `HAM_GUILD`, `AM_PKATTACK`

#### 11.4 `cmd/server/spawning.go` — 怪物重生
- `SpawnMonsters()` — 地图加载时初始刷新
- `RegenMonsters()` — 间隔后重生死亡怪物
- 刷新点配置来自 `MonGen.txt`
- 每地图人口上限
- 刷新区域（范围内随机位置）

### 交付物
怪物在地图上刷新、游荡、检测玩家、追逐和攻击。战斗按正确伤害公式结算。玩家可击杀怪物获得经验值。

---

## Phase 12: 服务端 — 魔法系统

**目标**: 实现Delphi中的所有30+种法术

**复杂度**: XL | **预计**: 3-4周

### 任务

#### 12.1 `cmd/server/magic.go` — 魔法管理器
移植 `Magic.pas`：
- `MagicManager` 结构体
- `DoSpell(player *PlayObject, magic *UserMagic, targetX, targetY int, target *BaseObject) bool` — 主法术分发
- `MPow(magic *UserMagic) int` — 随机威力
- `GetPower(power int, magic *UserMagic) int` — 按等级缩放
- `GetRPow(val int) int` — lo/hi word之间随机
- `CheckAmulet(player *PlayObject, count int, type int) bool`
- `UseAmulet(player *PlayObject, count int, type int)`
- `IsWarrSkill(magicID int) bool`

#### 12.2 `cmd/server/spells_warrior.go` — 战士技能
- `OneSword` — 基础剑术增强
- `PowerHit` — 重击（1.5倍伤害）
- `Ergum` — 刺杀剑术（前方扩展范围）
- `Banwol` — 半月弯刀（目标周围AoE）
- `FireSword` — 烈火剑法（额外火焰伤害）
- `CrossMoon` — 抱月刀法（十字AoE）
- `TwinBlade` — 双刀攻击
- `Mootebo` — 野蛮冲撞（冲锋攻击）

#### 12.3 `cmd/server/spells_wizard.go` — 法师技能
- `FireBall` (1) — 火球术（投射物火焰伤害）
- `Healing` (2) — 治愈术（自我治疗）
- `SummonSoul` — 召唤骷髅
- `Teleport` (8) — 随机传送
- `FireBang` (9) — 火焰爆炸（区域）
- `FireWall` (10) — 火墙（地面持续火焰）
- `ElecBlizzard` (11) — 雷电风暴（区域）
- `Lightning` (12) — 雷电术（单体）
- `MassHealing` (29) — 群体治疗
- `IceStorm` (33) — 冰风暴（区域）
- `Meteor` (34) — 流星术

#### 12.4 `cmd/server/spells_taoist.go` — 道士技能
- `SummonSkeleton` — 召唤骷髅
- `AmuletAttack` — 符咒攻击
- `Poisoning` — 施毒术
- `HolyCurtain` — 神圣屏障
- `TurnUndead` — 亡灵恐惧
- `Taming` — 驯服怪物
- `SummonAngel` — 召唤神兽
- `Invisibility` — 隐身术

#### 12.5 `cmd/server/spell_effects.go` — 法术效果
- `SpellEffect` 结构体: Type, X, Y, Duration, Target
- `ProcessEvents()` — 基于tick的效果处理
- 火墙: 每tick对站在上面的实体造成伤害
- 毒: 周期性HP流失
- 神圣屏障: 阻止移动
- 召唤: 创建/销毁宠物实体
- 传送: 带验证的空间移动

### 交付物
所有30+种法术功能正确。战士可使用近战技能，法师可释放火球和雷电，道士可召唤和治疗。法术效果在地图上持续并到期。

---

## Phase 13: 服务端 — NPC & 脚本

**目标**: 实现NPC系统，包含商店和任务脚本

**复杂度**: XL | **预计**: 3-4周

### 任务

#### 13.1 `cmd/server/npc.go` — NPC 基类
移植 `ObjNpc.pas`：
- `NormNpc` 结构体扩展AnimalObject
- 脚本引擎: `ScriptList []ScriptRecord`
- `ScriptRecord` 结构体: Label string, Procedures []ScriptProcedure
- `ScriptProcedure` 结构体: Conditions []Condition, Actions []Action, SayMsg string, ElseActions []Action, ElseSayMsg string
- `Click(player *PlayObject)` — NPC交互开始
- `UserSelect(player *PlayObject, data string)` — 玩家选择
- `GotoLabel(player *PlayObject, label string, extJump bool)` — 脚本流程
- `LoadNpcScript(filename string) error` — 解析NPC脚本文件

#### 13.2 `cmd/server/npc_script.go` — 脚本引擎
- 条件评估器（60+条件）:
  - `CHECKLEVEL`, `CHECKJOB`, `CHECKITEM`, `CHECKGOLD`, `CHECKPKPOINT`
  - `CHECKMAGIC`, `CHECKHUM`, `CHECKMAP`, `ISGUILDMASTER`
  - `CHECKNAMELIST`, `CHECK`（通用属性检查）
- 动作执行器（80+动作）:
  - `CHANGLEVEL`, `GIVEITEM`, `ADDSKILL`, `CHANGJOB`
  - `MOBPLACE`（刷新怪物）, `RECALLGROUP`, `GAMEGOLD`
  - `KILL`, `KICK`, `MAPMOVE`, `SENDMSG`
- 基于标签的流程: `#LABEL`, `#ACT`, `#SAY`, `#ELSEACT`, `#ELSESAY`
- `GOTO` 在标签间跳转

#### 13.3 `cmd/server/merchant.go` — 商人NPC
移植 `ObjNpc.pas` TMerchant：
- 扩展NormNpc
- `GoodsList []*MerchantGoods` — 出售物品
- `RefillGoodsList` — 自动补货物品
- `ItemPriceList` — 价格缓存
- 商店方法:
  - `ClientBuyItem(player *PlayObject, itemIdx, count int)` — 向玩家出售
  - `ClientSellItem(player *PlayObject, itemIdx int)` — 从玩家购买
  - `ClientRepairItem(player *PlayObject, itemIdx int)` — 修理服务
  - `ClientQuerySellPrice(player *PlayObject, itemIdx int)` — 价格查询
  - `ClientMakeDrugItem(player *PlayObject, itemIdx int)` — 药水制作
- 标志: Buy, Sell, MakeDrug, Storage, Getback, Repair, Upgradenow
- 仓库系统: 玩家可在商人处存取物品

#### 13.4 `cmd/server/npc_guild.go` — 行会NPC
- GuildOfficial NPC: 行会创建、成员管理
- CastleOfficial NPC: 城堡管理
- Trainer NPC: 技能训练假人

### 交付物
NPC站在地图上，点击后显示对话，出售/购买物品，执行任务脚本。玩家可购物、存取物品、完成简单任务。

---

## Phase 14: 集成 — 登录→游戏流程

**目标**: 将客户端和服务端连接起来，实现完整的登录到游戏流程（单端口统一架构）

**复杂度**: XL | **预计**: 3-4周

### 架构说明

本项目采用单端口统一监听架构，取代原 Delphi 的 8 进程拓扑：
- server 监听一个端口（默认 7000），内部自动路由登录/角色/游戏消息
- 客户端不需要切换连接，全流程在一个 TCP 连接上完成
- 账号、角色、行会数据统一存储在 SQLite 数据库中

### 任务

#### 14.1 `cmd/server/auth.go` — 认证模块（原 LoginSrv 功能）
- 处理 `CM_IDPASSWORD` 消息，验证账号/密码
- 查询 `server_data/accounts.db` 数据库
- 返回 `SM_PASSOK_SELECTSERVER` 或 `SM_PASSWD_FAIL`
- 会话管理：SessionID 生成、验证、超时

#### 14.2 `cmd/server/charmgr.go` — 角色管理模块（原 DBServer 功能）
- 处理 `CM_QUERYCHR` 消息，查询角色列表
- 处理 `CM_NEWCHR` 消息，创建角色
- 处理 `CM_DELCHR` 消息，删除角色
- 处理 `CM_SELCHR` 消息，选择角色进入游戏
- 查询 `server_data/characters.db` 数据库
- 返回 `SM_QUERYCHR`, `SM_STARTPLAY` 等响应

#### 14.3 `cmd/server/session.go` — 会话状态机
- 客户端连接状态：`Connected → Authenticated → CharacterSelected → InGame`
- 根据状态路由消息到不同处理器
- 断线重连处理
- 会话超时清理

#### 14.4 `cmd/server/gamedata.go` — 游戏数据初始化
- 从 `server_config/` 加载所有 JSONC 配置
- 初始化物品定义、怪物定义、魔法定义
- 加载地图数据
- 刷新初始怪物
- 放置 NPC 和商人

#### 14.5 完整集成测试
- 启动 server 进程（端口 7000）
- 客户端连接到 server:7000
- 登录流程: 认证 → 角色列表 → 选择角色 → 进入游戏
- 玩家出现在地图上，可四处走动
- 其他玩家可见（多客户端时）

### 交付物
端到端登录流程正常工作。玩家可创建账号、创建角色、选择角色并进入游戏世界。移动在客户端间同步。全流程在单一端口上完成。

---

## Phase 15: 社交系统

**目标**: 聊天、组队、行会、交易和PK系统

**复杂度**: L | **预计**: 2-3周

### 任务

#### 15.1 聊天系统
- 全频聊天、地图聊天、私聊、组队聊天
- 聊天消息广播与客户端自动换行
- `CM_SAY` → 服务端处理 → `SM_HEAR` 发送给附近玩家
- 客户端聊天历史（`DrawScrn.ChatStrs`）

#### 15.2 组队系统
- `CM_CREATEGROUP`, `CM_ADDGROUPMEMBER`, `CM_DELGROUPMEMBER`
- 客户端显示组队HP条
- 组队内共享经验值
- 组队聊天频道

#### 15.3 行会系统
- 通过NPC创建行会
- 行会名称、成员列表、职位
- 使用 SQLite 存储行会数据（`server_data/guilds.db`）
- `CM_OPENGUILDDLG`, `CM_GUILDADDMEMBER`, `CM_GUILDDELMEMBER`
- 行会聊天频道

#### 15.4 交易系统
- `CM_DEALTRY` → 请求与目标玩家交易
- `CM_DEALADDITEM` → 向交易窗口添加物品
- `CM_DEALEND` → 确认交易
- 双方确认后交易完成
- 客户端交易UI

#### 15.5 PK系统
- 击杀玩家时累积 `PKPoint`
- `PKFlag > 0` 时红名
- PK积分随时间衰减
- 安全区（不允许PK）
- 死亡时掉落物品

### 交付物
玩家可聊天、组队、创建行会、交易物品和进行PvP。所有社交功能端到端正常工作。

---

## Phase 16: 进阶系统

**目标**: 攻城战、武器升级、仓库、召唤和事件

**复杂度**: L | **预计**: 2-3周

### 任务

#### 16.1 城堡系统
- `UserCastle` 结构体: 所有者行会、防守方、进攻方
- 攻城战: 定时攻击、旗帜占领
- 城堡NPC管理（城堡专属商人）
- 从城堡商人收取税收

#### 16.2 武器升级系统
- 武器升级NPC（TMerchant，`m_boUpgradenow`）
- `UpgradeWeaponList` — 待升级队列
- 升级成功时随机属性提升
- 失败时销毁武器

#### 16.3 物品仓库
- 商人NPC处的每玩家仓库（50格）
- `CM_STORAGEITEM`, `CM_TAKEBACKSTORAGEITEM`
- 角色存档数据中的持久化仓库

#### 16.4 随机物品系统
- `RandomUpgradeItem()` — 怪物掉落时随机属性加成
- `RandomUpgradeUnknownItem()` — 未鉴定物品
- 通过服务端配置可配置升级率

#### 16.5 召唤系统
- `MakeSlave()` — 创建召唤怪物
- 召唤物跟随主人，攻击主人的目标
- 每职业召唤数量上限
- 召唤物经验和进化

#### 16.6 地图事件
- `MapEvent` 结构体: type, position, duration, effect
- 事件类型: 火焰、毒云、神圣屏障、陷阱
- 基于tick的事件处理
- 到期时清理事件

### 交付物
攻城战正常工作，武器可升级，物品可存储，召唤物跟随主人，地图事件持续并正确到期。

---

## Phase 17: 打磨 & 内容

**目标**: 音效、配置、管理工具、优化和测试

**复杂度**: M | **预计**: 2-3周

### 任务

#### 17.1 音效系统
- `SoundManager` 结构体，支持音频播放
- 音效: 脚步声、攻击声、法术声、UI点击声
- 每地图背景音乐
- 音量控制

#### 17.2 配置加载
- `!setup.txt` — 完整服务端配置
- `String.ini` — 210条服务端提示文本
- `Exps.ini` — 每等级经验表
- `Command.ini` — 管理员命令
- `BaseAbil.ini` — 每职业/等级基础属性

#### 17.3 管理员命令
- `@GOTO` — 传送到玩家
- `@RECALL` — 将玩家传送到你
- `@KICK` — 断开玩家
- `@MAKE` — 创建物品
- `@LEVEL` — 设置等级
- `@SABUKWALLGOLD` — 城堡金币
- `@MOB` — 刷新怪物

#### 17.4 性能优化
- 纹理图集: 合并小纹理为图集
- 空间索引: 基于网格的实体查找（替代线性扫描）
- 消息批处理: 合并多个小网络消息
- Goroutine池用于并发怪物AI处理
- 使用 `pprof` 进行性能分析引导优化

#### 17.5 客户端打磨
- FPS计数器显示
- 连接状态指示器
- 重连处理
- 错误消息和恢复
- ImGui调试覆盖层（F12切换）

#### 17.6 测试
- 所有 `internal/` 包的单元测试
- 客户端↔服务端消息流集成测试
- 负载测试: 100并发客户端
- 内存泄漏检测
- 跨平台构建验证（Windows + Linux）

### 交付物
生产就绪的游戏，包含音效、配置、管理工具和优化性能。通过所有单元和集成测试。

---

## 最终包结构

```
mirgo/
├── go.mod
├── cmd/
│   ├── client/              # 游戏客户端
│   │   ├── main.go
│   │   ├── scene_intro.go
│   │   ├── scene_login.go
│   │   ├── scene_selchr.go
│   │   ├── scene_notice.go
│   │   ├── scene_play.go        # 地图渲染 + 光照
│   │   ├── actor.go             # 角色基类 + 动画
│   │   ├── actor_hum.go         # 人类角色
│   │   ├── actor_npc.go         # NPC角色
│   │   ├── actor_monster.go     # 怪物角色
│   │   ├── actor_manager.go     # 角色生命周期
│   │   ├── input.go             # 输入处理
│   │   ├── action_queue.go      # 动作锁系统
│   │   ├── targeting.go         # 目标选择
│   │   ├── lighting.go          # 光照/雾效系统
│   │   ├── minimap.go           # 小地图覆盖层
│   │   ├── net_handler.go       # 服务端消息处理器
│   │   └── net_send.go          # 客户端消息发送器
│   ├── server/              # 游戏服务端（单端口统一架构）
│   │   ├── main.go
│   │   ├── config.go            # 服务端配置（JSONC）
│   │   ├── auth.go              # 认证模块（原 LoginSrv）
│   │   ├── charmgr.go           # 角色管理（原 DBServer）
│   │   ├── session.go           # 会话状态机
│   │   ├── runsocket.go         # 网络引擎
│   │   ├── frnengine.go         # DB I/O引擎（SQLite）
│   │   ├── usrengine.go         # 世界核心引擎
│   │   ├── baseobject.go        # TBaseObject
│   │   ├── playobject.go        # TPlayObject
│   │   ├── envir.go             # 地图环境
│   │   ├── mapmanager.go        # 地图管理器
│   │   ├── doors.go             # 门系统
│   │   ├── inventory.go         # 背包系统
│   │   ├── itemdef.go           # 物品定义
│   │   ├── monster.go           # 怪物基类
│   │   ├── monster_types.go     # 怪物变体
│   │   ├── combat.go            # 战斗系统
│   │   ├── spawning.go          # 怪物重生
│   │   ├── magic.go             # 魔法管理器
│   │   ├── spells_warrior.go    # 战士技能
│   │   ├── spells_wizard.go     # 法师技能
│   │   ├── spells_taoist.go     # 道士技能
│   │   ├── spell_effects.go     # 持续效果
│   │   ├── npc.go               # NPC基类
│   │   ├── npc_script.go        # 脚本引擎
│   │   ├── merchant.go          # 商店NPC
│   │   └── npc_guild.go         # 行会NPC
│   ├── serverconfig/        # 配置转换工具
│   │   ├── main.go
│   │   ├── parsers.go           # 通用解析器
│   │   ├── convert_server.go    # 主配置转换
│   │   ├── convert_exp.go       # 经验表转换
│   │   ├── convert_database.go  # SQLite数据库转换
│   │   ├── convert_maps.go      # 地图配置转换
│   │   ├── convert_items.go     # 物品配置转换
│   │   ├── convert_monsters.go  # 怪物配置转换
│   │   ├── convert_npcs.go      # NPC配置转换
│   │   └── convert_misc.go      # 其他配置转换
│   ├── mapviewer/           # (已存在)
│   └── wilviewer/           # (已存在)
├── internal/
│   ├── log/                 # (已存在)
│   ├── mapformat/           # (已存在)
│   ├── wil/                 # (已存在)
│   ├── protocol/            # 新增: 共享协议
│   │   ├── edcode.go
│   │   ├── message.go
│   │   ├── types.go
│   │   └── types_test.go
│   ├── engine/              # 新增: 共享渲染引擎
│   │   ├── window.go
│   │   ├── gl.go
│   │   ├── shader.go
│   │   ├── camera.go
│   │   └── resourcemanager.go
│   └── netclient/           # 新增: TCP客户端库
│       └── client.go
├── server_config/           # 转换后的JSONC配置文件
│   ├── server.jsonc           # 主配置
│   ├── exp_table.jsonc        # 经验值表
│   ├── strings.jsonc          # 提示文本
│   ├── maps/                  # 地图配置和.map文件
│   ├── items/                 # 物品定义
│   ├── monsters/              # 怪物定义
│   ├── magic/                 # 魔法定义
│   └── npcs/                  # NPC脚本和配置
├── server_data/             # 运行时数据（SQLite数据库）
│   ├── accounts.db            # 账号数据库
│   ├── characters.db          # 角色存档数据库
│   └── guilds.db              # 行会数据库
└── asset/                   # (已gitignore)
    ├── client/
    ├── server/
    └── delphi/
```

---

## 复杂度汇总

| 阶段 | 名称 | 复杂度 | 预计周期 |
|------|------|--------|----------|
| Phase 1 | 基础层 — 共享数据 & 协议 | M | 1-2周 |
| Phase 2 | 客户端 — 窗口 & 场景框架 | M | 1-2周 |
| Phase 3 | 客户端 — 游戏场景 & 地图渲染 | L | 2-3周 |
| Phase 4 | 客户端 — 角色系统 | XL | 3-4周 |
| Phase 5 | 客户端 — 输入 & 移动 | M | 1-2周 |
| Phase 6 | 客户端 — 网络层 | L | 2周 |
| Phase 7 | 服务端 — 核心基础设施 | XL | 3-4周 |
| Phase 8 | 客户端 — 光照 & 雾效 | M | 1周 |
| Phase 9 | 服务端 — 地图 & 世界管理 | L | 2周 |
| Phase 10 | 服务端 — 玩家逻辑 | XL | 3-4周 |
| Phase 11 | 服务端 — 怪物AI & 战斗 | XL | 3-4周 |
| Phase 12 | 服务端 — 魔法系统 | XL | 3-4周 |
| Phase 13 | 服务端 — NPC & 脚本 | XL | 3-4周 |
| Phase 14 | 集成 — 登录→游戏流程 | XL | 3-4周 |
| Phase 15 | 社交系统 | L | 2-3周 |
| Phase 16 | 进阶系统 | L | 2-3周 |
| Phase 17 | 打磨 & 内容 | M | 2-3周 |

**总预计工作量**: 35-50周（8-12个月），单人开发

---

## 关键设计决策

1. **`internal/protocol/` 集中共享类型** — 客户端和服务端从同一包导入，保证线路兼容性。Delphi在 `Common/Grobal2.pas` 中重复类型；我们集中管理。

2. **`internal/engine/` 共享渲染** — 从mapviewer/wilviewer提取OpenGL样板代码到共享包。客户端和两个查看器都从这里导入。

3. **单端口统一监听** — server 监听一个端口（默认7000），内部自动路由登录/角色/游戏消息，客户端无需切换连接。取代原 Delphi 的 8 进程拓扑。

4. **每连接一个goroutine** — 每个客户端连接有自己的goroutine。通过channel向游戏引擎线程传递消息。

5. **单线程游戏循环** — M2Server主循环以10 ticks/秒在单个goroutine中运行（匹配Delphi的定时器方式）。避免游戏状态的并发修改。重I/O（DB）通过FrontEngine在独立goroutine中运行。

6. **SQLite 数据存储** — 账号、角色、行会数据统一存储在 SQLite 数据库中（`server_data/` 目录），单文件便于备份和管理。取代原 Delphi 的平面文件存储。

7. **场景接口** — 清晰的UI状态分离。每个场景管理自己的渲染和输入。场景转换是显式的。

8. **客户端动作锁定** — 必须等待服务器确认后才能发送下一个动作。这对防止不同步至关重要，匹配Delphi的反作弊机制。

9. **6Bit编码保真** — 精确重新实现Delphi编码，不做重新设计。确保与现有工具和测试向量的兼容性。

10. **不使用外部游戏引擎** — 自定义OpenGL渲染（匹配现有mapviewer方式）。保持依赖最小化并完全控制渲染管线。

---

## 验证方案

每个阶段的验证方式：

| 阶段 | 验证方式 |
|------|----------|
| Phase 1 | 单元测试: 编码往返测试，结构体大小验证 |
| Phase 2 | 运行客户端，窗口打开，WIL资源加载日志 |
| Phase 3 | 运行客户端，渲染完整地图，与mapviewer截图对比 |
| Phase 4 | 运行客户端，调试命令创建测试角色，验证动画帧 |
| Phase 5 | 运行客户端，点击移动，碰撞验证，动作锁测试 |
| Phase 6 | 单元测试: 消息编解码，连接测试服务器 |
| Phase 7 | 启动服务端，监听端口，日志记录收到的消息 |
| Phase 8 | 运行客户端，黑暗地图光照效果，火把照明 |
| Phase 9 | 启动服务端，加载所有地图，空间查询日志 |
| Phase 10 | 服务端处理登录消息，角色数据加载/保存 |
| Phase 11 | 怪物刷新、追逐、攻击，伤害公式验证 |
| Phase 12 | 各职业法术施放，效果持续和到期 |
| Phase 13 | NPC对话、商店交易、任务脚本执行 |
| Phase 14 | 端到端: 登录→角色选择→进入游戏→移动 |
| Phase 15 | 聊天、组队、行会、交易、PvP |
| Phase 16 | 攻城战、武器升级、仓库、召唤 |
| Phase 17 | 全面测试，负载测试，跨平台构建 |

---

# 附录 A：Delphi 服务端配置文件分析

## 目录结构概览

```
asset/server/
├── !setup.txt              # 主服务器配置（INI格式，81KB，3565行）
├── BaseAbil.ini            # 基础属性配置（空文件）
├── Command.ini             # GM命令定义（INI格式，7.5KB，392行）
├── Exps.ini                # 经验值表（INI格式，93KB，5133行）
├── GlobalVal.ini           # 全局变量（INI格式，31KB，2001行）
├── SendSMS.ini             # 短信验证配置（阿里云，169字节）
├── String.ini              # 服务端提示文本（INI格式，20KB，489行）
├── 系统插件.ini              # 系统插件开关（88字节，6行）
├── Castle\                 # 城堡（沙巴克）配置
│   ├── List.txt            # 城堡列表
│   └── 0\
│       ├── AttackSabukWall.txt
│       └── SabukW.txt      # 沙巴克城堡详细配置（INI格式，2.4KB，177行）
├── Envir\                  # 游戏环境配置（核心目录）
│   ├── AdminList.txt       # 管理员列表（64字节，10行）
│   ├── mapinfo.txt         # 地图信息（88KB，3831行）
│   ├── merchant.txt        # 商人/NPC位置（11KB，287行）
│   ├── mongen.txt          # 怪物刷新点（17KB，4146行）
│   ├── Npcs.txt            # NPC定义（2.2KB，46行）
│   ├── GuardList.txt       # 卫兵列表（2.5KB，95行）
│   ├── StartPoint.txt      # 安全区/复活点（314字节，13行）
│   ├── MiniMap.txt         # 小地图映射（2.1KB，228行）
│   ├── MakeItem.txt        # 合成配方（2.6KB，168行）
│   ├── FilterItemList.txt  # 物品过滤列表（20KB，687行）
│   ├── ItemRuleList.txt    # 物品规则列表（71KB，625行）
│   ├── GroupItemList.txt   # 套装定义（5.2KB，15行）
│   ├── UnbindList.txt      # 解绑列表（406字节，18行）
│   ├── CustomHeroMagic.ini # 英雄自定义魔法（3.8KB，326行）
│   ├── Boxs\               # 宝箱配置
│   ├── CustomMagic\        # 自定义魔法（13个.ini文件）
│   ├── MapQuest_def\       # 地图任务脚本（9个.txt文件）
│   ├── market_def\         # 商人脚本（100+个.txt文件）
│   ├── MonItems\           # 怪物掉落表（200+个.txt文件）
│   ├── MonUseItems\        # 怪物装备配置（36个.txt文件）
│   ├── Npc_def\            # NPC脚本（30+个.txt文件）
│   ├── Robot_def\          # 机器人脚本（空）
│   └── SmartMonster\       # 智能怪物配置（60+个.ini文件）
├── GuildBase\              # 行会数据
│   └── Guildlist.txt       # 行会列表（空）
├── Map\                    # 地图二进制文件（500+个.map）
├── Notice\                 # 公告文件
│   ├── Notice.txt          # 登录公告（161字节，17行）
│   └── LineNotice.txt      # 滚动公告（空）
└── 数据库\                  # SQLite数据库
    ├── GEEM2.db            # SQLite数据库文件（21KB）
    └── GEEM2.db.sql        # 数据库结构和数据SQL（20KB，1310行）
```

## 根目录配置文件详解

### 1. `!setup.txt` — 主服务器配置

- **格式**: INI 风格（`[Section]` + `Key=Value`）
- **大小**: 81,181 字节，约 3,565 行
- **编码**: GBK/GB2312

**主要 Section**：
```ini
[Server]
ServerIndex=0
ServerName=服务器名
DBAddr=127.0.0.1        # DBServer地址（本项目移除）
DBPort=6000              # DBServer端口（本项目移除）
IDSAddr=127.0.0.1        # LoginSrv地址（本项目移除）
IDSPort=5600             # LoginSrv端口（本项目移除）
GateAddr=0.0.0.0         # 网关地址（本项目移除）
GatePort=5000            # 网关端口（本项目改为统一端口）
HumLimit=30              # 人类处理限制
MonLimit=30              # 怪物处理限制
UserFull=10000           # 最大在线人数

[Setup]
HomeMap=0                # 出生地图
HomeX=289                # 出生X坐标
HomeY=618                # 出生Y坐标
GroupMembersMax=10       # 组队最大人数
BuildGuild=1000000       # 创建行会费用
GuildWarFee=30000        # 行会战费用
# ... 数百个游戏设置项
```

### 2. `Command.ini` — GM 命令定义

- **格式**: INI 风格
- **大小**: 7,581 字节，392 行

**Section 结构**：
```ini
[Command]                    # 命令名称映射
Date=Date
PrvMsg=PrvMsg
Move=Move
Mob=Mob
Kill=Kill
Make=make
ReloadNpc=ReloadNpc

[Permission]                 # 每个命令的权限等级（0=普通玩家, 10=最高GM）
Date=0
Mob=10
Kill=10
MakeMin=10

[Setup]                      # 命令附加设置
```

### 3. `Exps.ini` — 经验值表

- **格式**: INI 风格
- **大小**: 93,095 字节，5,133 行

```ini
[Exp]
HighLevel=1000
KillMonExpMultiple=1
BaseExp=100000000
Level1=100
Level2=200
Level3=300
...
Level39=9000000
```

### 4. `String.ini` — 服务端提示文本

- **格式**: INI 风格
- **大小**: 20,546 字节，489 行
- **内容**: 约 210 条游戏内提示消息

```ini
[String]
ClientSoftVersionError=游戏版本错误!
OnlineUserFull=服务器已满!
AttackModeOfAll=[攻击模式: 全体攻击]
AttackModeOfPeaceful=[攻击模式: 和平攻击]
ThrustingOn=开启刺杀剑术
TheWeaponBroke=武器损坏了!
```

### 5. `GlobalVal.ini` — 全局变量

- **格式**: INI 风格
- **大小**: 31,788 字节，2,001 行
- **内容**: 2000 个全局变量 `GlobalVal0` ~ `GlobalVal1999`，全部初始化为 0

### 6. `BaseAbil.ini` — 基础属性配置

- **格式**: 空文件（0 字节）
- **说明**: 预留但未使用

## `Envir/` 目录配置文件详解

### 7. `mapinfo.txt` — 地图信息定义

- **格式**: 自定义格式
- **大小**: 88,605 字节，3,831 行

**格式说明**：
```
;地图编号 地图名称 标志 [属性标记]
[0 比奇省 0]
[0122 宫殿 0] DAY
[0137 竞技之地 0] NODRUG

;传送点格式: 源地图 源坐标 -> 目标地图 目标坐标
0    346,186  -> 0122    11,41
```

**属性标记**: `DAY`(白天), `NORECALL`(禁止召唤), `NORECONNECT(地图)`, `NODRUG`(禁止用药), `SAFE`(安全区), `FIGHT`(战斗区), `DARK`(黑暗地图)

### 8. `merchant.txt` — 商人/NPC 位置定义

- **格式**: 自定义 CSV 格式（Tab 分隔）
- **大小**: 11,032 字节，287 行

**格式说明**：
```
;ID    地图    X    Y    名称    正面    主要部分    沙巴克城堡(税前价格)
1Bme   0102    9    7    屠夫    0       11          0
2Bwe   0103   19   18    卫家店  0       1           0
```

**ID前缀含义**: `1`=肉店, `2`=武器店, `3`=服装店, `4`=药店, `5`=书店, `6`=客栈/仓库, `7`=杂货店, `8`=首饰店, `9`=特殊NPC

### 9. `mongen.txt` — 怪物刷新点定义

- **格式**: 自定义格式（空格分隔）
- **大小**: 17,759 字节，4,146 行

**格式说明**：
```
;地图   x     y     name     半径   数量   时间（分）
0      665   86    弓箭守卫  0      1      1
0      567   621   弓箭守卫  0      1      1
```

### 10. `Npcs.txt` — NPC 定义

- **格式**: 自定义格式（空格分隔）
- **大小**: 2,201 字节，46 行

**格式说明**：
```
;名字          race    地图      x     y     脸    身体
;race: 0=店主, 1=国王, 2=沙巴克城堡官员
国王            1       0122     29    32    0     8
传送员           0       0        297   218   0     15
```

### 11. `GuardList.txt` — 卫兵列表

- **格式**: 自定义格式
- **大小**: 2,509 字节，95 行

```
;名称  地图  [x,y]  : direction(0-7)
卫士   0    [409,189]  : 1
```

### 12. `StartPoint.txt` — 安全区/复活点

- **格式**: 自定义格式（空格/Tab 分隔）
- **大小**: 314 字节，13 行

```
;地图  X    Y    属性  范围
0     289  618  0     10
0     650  631  0     10
2     503  483  0     10
```

### 13. `MiniMap.txt` — 小地图映射

- **格式**: 简单的 `地图名  小地图编号` 映射
- **大小**: 2,124 字节，228 行

```
D001  1
D002  2
```

### 14. `MakeItem.txt` — 合成配方

- **格式**: 自定义格式
- **大小**: 2,633 字节，168 行

```
[灰色药粉(少量)]
食人树叶    4
毒蜘蛛牙齿  2
食人树的果实  1
```

### 15. `FilterItemList.txt` — 物品过滤列表

- **格式**: Tab 分隔的多列格式
- **大小**: 20,587 字节，687 行

```
;类型  名称    属性1  属性2  属性3  属性4  属性5  属性6
1      金币1   0      0      1      0      0      255
2      金创药(小量)  0  0  1  0  0  255
```

### 16. `ItemRuleList.txt` — 物品规则列表

- **格式**: 自定义格式（`名称\t规则位|规则位`）
- **大小**: 71,754 字节，625 行
- **内容**: 每个物品的详细规则（50+个布尔标志位）

### 17. `GroupItemList.txt` — 套装定义

- **格式**: Tab 分隔的复杂格式
- **大小**: 5,243 字节，15 行

```
;ID  套装数  名称  物品列表|分隔  属性  描述
1    8       天龙套装全  天龙神型夺命剑|天龙不死衣...  ...  天龙套装生效,防御+2-6...
```

### 18. `UnbindList.txt` — 解绑物品列表

- **格式**: `编号  物品名称`
- **大小**: 406 字节，18 行

```
100    强效金创药
101    强效魔法药
106    地牢逃脱卷
```

### 19. `AdminList.txt` — 管理员列表

- **格式**: `* 角色名`（每行一个）
- **大小**: 64 字节，10 行

```
* GM01
* GM02
```

## 子目录配置文件详解

### 20. `market_def/` — 商人脚本目录

- **文件数**: 100+ 个 .txt 文件
- **格式**: NPC 脚本语言（自定义脚本引擎）

**脚本格式**：
```
;第一行: 商店类型标签
武器
;第二行: 支持的操作
(@trading @buy @sell @makedrug @storage @getback @upgradenow @repair)
;主对话入口
[@main]
#IF
check [204] 0
#ACT
goto @main-2
```

**文件命名规则**: `类型前缀+城市标识-地图编号.txt`

### 21. `MonItems/` — 怪物掉落表目录

- **文件数**: 200+ 个 .txt 文件
- **格式**: 自定义掉落格式

**格式说明**：
```
;怪物名
;鸡
1/1 鸡肉

;白野猪
1/1 金币 5000
1/1 强效太阳水
1/2 强效太阳水
1/1 金创药(中量)
```

**概率格式**: `1/N` 表示 1/N 的概率掉落

### 22. `Npc_def/` — NPC 脚本目录

- **文件数**: 30+ 个 .txt 文件
- **格式**: NPC 脚本语言

**脚本格式**：
```
[@main]
#IF
条件检查
#SAY
对话内容\
 <选项/@标签>\

[@标签]
#IF
条件检查
#ACT
执行动作
#ELSESAY
失败对话
```

### 23. `SmartMonster/` — 智能怪物配置目录

- **文件数**: 60+ 个 .ini 文件
- **格式**: INI 风格
- **大小**: 每个约 20KB

**Section 结构**：
```ini
[ClientConfig]     ; 客户端渲染配置
[ClientSounds]     ; 音效配置
[ActStand]         ; 站立动作
[ActWalk]          ; 行走动作
[ActAttack]        ; 攻击动作
[ActStruck]        ; 受击动作
[ActDie]           ; 死亡动作

[ActStand]
ActionFile=22        ; 动画文件索引
StartIndex=520       ; 起始帧索引
PlayCount=6          ; 播放帧数
PlayTime=200         ; 播放时间(ms)
```

### 24. `CustomMagic/` — 自定义魔法目录

- **文件数**: 13 个 .ini 文件
- **格式**: INI 风格
- **大小**: 每个约 38KB

**包含的魔法**: 冰焰术、剑气爆、嗜血术、天务、天霜冰环、护身气幕、毒云、深延术、火龙气焰、瘟疫、血龙水、诅咒术、阴阳盾

### 25. `MonUseItems/` — 怪物装备配置目录

- **文件数**: 36 个 .txt 文件
- **格式**: INI 风格
- **文件命名规则**: `等级性别职业.txt`（如 `一六男战.txt` = 16级男战士）

```ini
[Info]
Job=0           ; 职业: 0=战士
Gender=0        ; 性别: 0=男
Hair=2          ; 发型
UseSkill=攻杀剑术,刺杀剑术,  ; 使用的技能
```

### 26. `Castle/` — 城堡配置

**`SabukW.txt` 格式**：
```ini
[setup]
CastleName=沙巴克
OwnGuild=
TotalGold=0

[defense]
CastleMap=3
MainDoorName=SabukDoor
MainDoorX=672
MainDoorY=330
MainDoorHP=10000
LeftWallHP=5000
CenterWallHP=5000
RightWallHP=5000
```

### 27. `Notice/` — 公告文件

- `Notice.txt` — 登录公告（161 字节，17 行）
- `LineNotice.txt` — 滚动公告（空）

### 28. `数据库/` — SQLite 数据库

**数据库表结构**：
```sql
-- 物品表
CREATE TABLE "StdItems" (
    Idx INTEGER, Name TEXT(14), Stdmode INTEGER, Shape INTEGER,
    Weight INTEGER, Looks INTEGER, DuraMax INTEGER,
    Ac INTEGER, Ac2 INTEGER, Mac INTEGER, Mac2 INTEGER,
    Dc INTEGER, Dc2 INTEGER, Mc INTEGER, Mc2 INTEGER,
    Sc INTEGER, Sc2 INTEGER, Need INTEGER, NeedLevel INTEGER,
    Price INTEGER
);

-- 怪物表
CREATE TABLE "Monster" (
    Name TEXT(14), Race INTEGER, RaceImg INTEGER, Appr INTEGER,
    Lvl INTEGER, Undead INTEGER, Exp INTEGER, HP INTEGER,
    AC INTEGER, MAC INTEGER, DC INTEGER, DCMAX INTEGER,
    MC INTEGER, SC INTEGER, Speed INTEGER, Hit INTEGER
);

-- 魔法表
CREATE TABLE "Magic" (
    MagID INTEGER, MagName TEXT(12), EffectType INTEGER,
    Effect INTEGER, Spell INTEGER, Power INTEGER, MaxPower INTEGER,
    Job INTEGER, NeedL1 INTEGER, L1Train INTEGER, ...
);
```

## 文件格式分类总结

| 格式类型 | 文件数 | 代表文件 |
|---|---|---|
| **INI 风格** | ~80+ | !setup.txt, Command.ini, Exps.ini, String.ini, SmartMonster/*.ini, CustomMagic/*.ini |
| **自定义脚本语言** | ~140+ | market_def/*.txt, Npc_def/*.txt, MapQuest_def/*.txt |
| **自定义数据格式** | ~250+ | mapinfo.txt, merchant.txt, mongen.txt, MonItems/*.txt, StartPoint.txt |
| **SQL** | 1 | GEEM2.db.sql |
| **二进制** | ~501 | Map/*.map, GEEM2.db |
| **空文件/占位** | ~20+ | BaseAbil.ini, 多个 Deny*.txt, Disable*.txt |

## 关键数据定义文件索引

| 数据类型 | 定义文件 | 格式 |
|---|---|---|
| **物品** | `数据库/GEEM2.db` → StdItems 表 | SQLite |
| **怪物** | `数据库/GEEM2.db` → Monster 表 | SQLite |
| **魔法** | `数据库/GEEM2.db` → Magic 表 | SQLite |
| **地图** | `Envir/mapinfo.txt` + `Map/*.map` | 文本+二进制 |
| **NPC** | `Envir/Npcs.txt` + `Envir/merchant.txt` | 自定义文本 |
| **商人** | `Envir/market_def/*.txt` | NPC脚本 |
| **怪物刷新** | `Envir/mongen.txt` | 自定义文本 |
| **怪物掉落** | `Envir/MonItems/*.txt` | 自定义文本 |
| **经验表** | `Exps.ini` | INI |
| **服务器设置** | `!setup.txt` | INI |
| **GM命令** | `Command.ini` | INI |
| **套装** | `Envir/GroupItemList.txt` | 自定义文本 |
| **合成配方** | `Envir/MakeItem.txt` | 自定义文本 |
| **城堡** | `Castle/0/SabukW.txt` | INI |
| **安全区** | `Envir/StartPoint.txt` | 自定义文本 |
| **卫兵** | `Envir/GuardList.txt` | 自定义文本 |
| **怪物行为** | `Envir/SmartMonster/*.ini` | INI |
| **自定义技能** | `Envir/CustomMagic/*.ini` | INI |

---

# 附录 B：Server Config Converter 工具

## 目标

创建一个 CLI 工具 `cmd/serverconfig/main.go`，将 Delphi 服务端配置文件（`asset/server/`）转换为本项目统一的 JSONC 格式，输出到 `server_config/` 目录。

## 背景

- Delphi 原项目有 8 个独立进程（LoginGate、SelGate、RunGate、LoginSrv、DBServer、M2Server 等），各自有独立配置
- 本项目合并为单一 `cmd/server` 进程，需要将分散的配置统一为一套 JSONC 配置
- `asset/server/Map/*.map` 二进制地图文件直接复制，不做转换
- 其余配置文件全部转为 JSONC 格式

## 源文件格式分类

| 格式 | 文件 | 解析策略 |
|------|------|----------|
| **INI** | `!setup.txt`, `Command.ini`, `Exps.ini`, `String.ini`, `GlobalVal.ini`, `系统插件.ini`, `SendSMS.ini` | 通用 INI 解析器 |
| **SQLite** | `数据库/GEEM2.db`（StdItems、Monster、Magic 表） | `database/sql` 读取 |
| **自定义文本** | `mapinfo.txt`, `merchant.txt`, `mongen.txt`, `Npcs.txt`, `GuardList.txt`, `StartPoint.txt`, `MiniMap.txt`, `MakeItem.txt`, `UnbindList.txt`, `FilterItemList.txt`, `GroupItemList.txt`, `ItemRuleList.txt`, `AdminList.txt` 等 | 每个文件专用解析器 |
| **NPC 脚本** | `market_def/*.txt`, `Npc_def/*.txt`, `MapQuest_def/*.txt` | 保持原始脚本格式，包装为 JSON |
| **INI 子目录** | `SmartMonster/*.ini`, `CustomMagic/*.ini`, `MonUseItems/*.txt`, `CustomHeroMagic.ini` | 通用 INI 解析器 |
| **掉落表** | `MonItems/*.txt` | 专用解析器 |
| **城堡** | `Castle/0/SabukW.txt` | INI 解析器 |
| **公告** | `Notice/Notice.txt`, `Notice/LineNotice.txt` | 纯文本包装 |
| **二进制** | `Map/*.map` | 直接复制 |

## 输出目录结构

```
server_config/
├── server.jsonc           # 合并后的服务端主配置（来自 !setup.txt + Command.ini + 系统插件.ini）
├── exp_table.jsonc        # 经验值表（来自 Exps.ini）
├── strings.jsonc          # 提示文本（来自 String.ini）
├── base_abil.jsonc        # 基础属性（来自 BaseAbil.ini + 数据库）
├── global_vars.jsonc      # 全局变量（来自 GlobalVal.ini）
├── maps/
│   ├── map_info.jsonc     # 地图信息（来自 mapinfo.txt）
│   ├── *.map              # 二进制地图文件（直接复制）
│   ├── mini_map.jsonc     # 小地图映射
│   ├── start_points.jsonc # 安全区/复活点
│   └── doors.jsonc        # 门信息（从地图文件提取）
├── items/
│   ├── std_items.jsonc    # 物品定义（来自 SQLite StdItems 表）
│   ├── filter_items.jsonc # 物品过滤列表
│   ├── item_rules.jsonc   # 物品规则列表
│   ├── group_items.jsonc  # 套装定义
│   ├── unbind_list.jsonc  # 解绑列表
│   └── make_items.jsonc   # 合成配方
├── monsters/
│   ├── monster_db.jsonc   # 怪物定义（来自 SQLite Monster 表）
│   ├── mon_gen.jsonc      # 怪物刷新点（来自 mongen.txt）
│   ├── mon_items/         # 怪物掉落表（每个怪物一个文件）
│   ├── mon_use_items/     # 怪物装备配置
│   └── smart_monster/     # 智能怪物配置
├── magic/
│   ├── magic_db.jsonc     # 魔法定义（来自 SQLite Magic 表）
│   ├── custom_magic/      # 自定义魔法
│   └── hero_magic.jsonc   # 英雄魔法配置
├── npcs/
│   ├── npc_list.jsonc     # NPC 定义（来自 Npcs.txt）
│   ├── merchant_list.jsonc # 商人位置（来自 merchant.txt）
│   ├── npc_scripts/       # NPC 脚本（保持原始格式，包装为 JSON）
│   ├── merchant_scripts/  # 商人脚本
│   └── map_quest_scripts/ # 地图任务脚本
├── guards/
│   ├── guard_list.jsonc   # 卫兵列表
│   └── admin_list.jsonc   # 管理员列表
├── castle/
│   └── sabuk_wall.jsonc   # 沙巴克城堡配置
├── guild/
│   └── guild_list.jsonc   # 行会列表
├── notice/
│   ├── notice.jsonc       # 登录公告
│   └── line_notice.jsonc  # 滚动公告
└── misc/
    ├── effect_images.jsonc # 特效图像列表
    ├── eat_items.jsonc    # 可快速使用物品
    └── drop_lists.jsonc   # 掉落表生成数据
```

## 实现任务

### T1: 通用解析器模块 `cmd/serverconfig/parsers.go`

提供以下通用解析器：
- `ParseINI(filename string) (map[string]map[string]string, error)` — 通用 INI 解析
- `ParseSQLite(filename string) (*sql.DB, error)` — SQLite 数据库打开
- `ParseCustomTable(filename string, separator string) ([]map[string]string, error)` — 自定义分隔符表格
- `ParseLineList(filename string) ([]string, error)` — 纯行列表
- `ReadGBKFile(filename string) ([]byte, error)` — GBK 编码读取（转 UTF-8）
- `WriteJSONC(filename string, data interface{}, comment string) error` — JSONC 输出（带注释头）

### T2: 主配置转换 `cmd/serverconfig/convert_server.go`

- 读取 `!setup.txt` 的 `[Server]`、`[Setup]` 等 section
- 读取 `Command.ini` 的命令定义和权限
- 读取 `系统插件.ini` 的功能开关
- **移除多进程配置**：DBAddr、IDSAddr、GateAddr、DBPort、IDSPort 等地址/端口配置全部删除
- **新增单端口配置**：`listen.addr` = `0.0.0.0:7000`
- **新增数据库配置**：`database.accounts/characters/guilds` 路径
- 合并输出为 `server.jsonc`

server.jsonc 核心结构：
```jsonc
{
  "server": {
    "name": "服务器名",
    "index": 0,
    "listen": { "addr": "0.0.0.0:7000" },
    "limits": { "maxPlayers": 10000, "humLimit": 30, "monLimit": 30 }
  },
  "database": {
    "accounts": "server_data/accounts.db",
    "characters": "server_data/characters.db",
    "guilds": "server_data/guilds.db"
  },
  "game": {
    "homeMap": "0", "homeX": 289, "homeY": 618,
    "groupMembersMax": 10,
    "buildGuild": 1000000,
    "guildWarFee": 30000,
    // ... 其他游戏设置
  },
  "commands": { /* 来自 Command.ini */ },
  "plugins": { /* 来自 系统插件.ini */ }
}
```

### T3: 经验表和字符串 `cmd/serverconfig/convert_exp.go`

- 读取 `Exps.ini` → `exp_table.jsonc`
- 读取 `String.ini` → `strings.jsonc`
- 读取 `GlobalVal.ini` → `global_vars.jsonc`

### T4: 数据库转换 `cmd/serverconfig/convert_database.go`

- 读取 `数据库/GEEM2.db` 的三个表：
  - `StdItems` → `items/std_items.jsonc`
  - `Monster` → `monsters/monster_db.jsonc`
  - `Magic` → `magic/magic_db.jsonc`

### T5: 地图配置转换 `cmd/serverconfig/convert_maps.go`

- 读取 `mapinfo.txt` → `maps/map_info.jsonc`（解析地图编号、名称、属性、传送点）
- 复制 `Map/*.map` → `maps/*.map`（直接复制）
- 读取 `MiniMap.txt` → `maps/mini_map.jsonc`
- 读取 `StartPoint.txt` → `maps/start_points.jsonc`
- 读取地图文件提取门信息 → `maps/doors.jsonc`

### T6: 物品配置转换 `cmd/serverconfig/convert_items.go`

- 读取 `FilterItemList.txt` → `items/filter_items.jsonc`
- 读取 `ItemRuleList.txt` → `items/item_rules.jsonc`
- 读取 `GroupItemList.txt` → `items/group_items.jsonc`
- 读取 `UnbindList.txt` → `items/unbind_list.jsonc`
- 读取 `MakeItem.txt` → `items/make_items.jsonc`

### T7: 怪物配置转换 `cmd/serverconfig/convert_monsters.go`

- 读取 `mongen.txt` → `monsters/mon_gen.jsonc`
- 读取 `MonItems/*.txt` → `monsters/mon_items/*.jsonc`（每个怪物一个文件）
- 读取 `MonUseItems/*.txt` → `monsters/mon_use_items/*.jsonc`
- 读取 `SmartMonster/*.ini` → `monsters/smart_monster/*.jsonc`

### T8: NPC 配置转换 `cmd/serverconfig/convert_npcs.go`

- 读取 `Npcs.txt` → `npcs/npc_list.jsonc`
- 读取 `merchant.txt` → `npcs/merchant_list.jsonc`
- 复制 `market_def/*.txt` → `npcs/merchant_scripts/*.jsonc`（包装原始脚本）
- 复制 `Npc_def/*.txt` → `npcs/npc_scripts/*.jsonc`
- 复制 `MapQuest_def/*.txt` → `npcs/map_quest_scripts/*.jsonc`

### T9: 其他配置转换 `cmd/serverconfig/convert_misc.go`

- 读取 `GuardList.txt` → `guards/guard_list.jsonc`
- 读取 `AdminList.txt` → `guards/admin_list.jsonc`
- 读取 `Castle/0/SabukW.txt` → `castle/sabuk_wall.jsonc`
- 读取 `GuildBase/Guildlist.txt` → `guild/guild_list.jsonc`
- 读取 `Notice/Notice.txt` → `notice/notice.jsonc`
- 读取 `CustomMagic/*.ini` → `magic/custom_magic/*.jsonc`
- 读取 `CustomHeroMagic.ini` → `magic/hero_magic.jsonc`
- 读取 `EffectImageList.txt` → `misc/effect_images.jsonc`
- 读取 `ClientEatItemNameList.txt` → `misc/eat_items.jsonc`

### T10: 主入口 `cmd/serverconfig/main.go`

- 解析 CLI 参数：`--input asset/server --output server_config`
- 调用所有转换模块
- 输出统计信息（转换了多少文件，跳过了多少空文件）
- 错误处理和日志

## JSONC 格式规范

每个 JSONC 文件包含：
1. 顶部注释说明文件来源和用途
2. `_source` 字段记录原始文件路径
3. `_description` 字段记录数据说明
4. 实际数据

示例：
```jsonc
{
  // 物品定义表
  // 来源: asset/server/数据库/GEEM2.db → StdItems 表
  // 说明: 所有物品的基础属性模板

  "_source": "asset/server/数据库/GEEM2.db",

  "items": [
    {
      "idx": 1,
      "name": "金币",
      "stdMode": 0,
      "shape": 0,
      "weight": 0,
      "looks": 100,
      "duraMax": 0,
      "ac": 0, "acMax": 0,
      "mac": 0, "macMax": 0,
      "dc": 0, "dcMax": 0,
      "mc": 0, "mcMax": 0,
      "sc": 0, "scMax": 0,
      "need": 0,
      "needLevel": 0,
      "price": 0
    }
  ]
}
```

## NPC 脚本处理

NPC 脚本保持原始格式，包装为 JSON：
```jsonc
{
  "_source": "asset/server/Envir/market_def/2Bme-0102.txt",
  "_type": "merchant_script",
  "_map": "0102",
  "_npcName": "武器店",

  "operations": ["@trading", "@buy", "@sell", "@repair"],
  "script": "原始脚本内容保持不变..."
}
```

## 单进程架构设计

### 原 Delphi 8 进程拓扑 → 本项目单进程

| 原进程 | 端口 | 原职责 | 本项目合并方式 |
|--------|------|--------|----------------|
| LoginGate | 7000 | TCP代理，转发到LoginSrv | 合并到 server 监听端口 |
| SelGate | 7100 | TCP代理，转发到DBServer | 合并到 server 监听端口 |
| RunGate | 7200-7900 | TCP代理，转发到M2Server（最多8实例） | 合并到 server 监听端口 |
| LoginSrv | 5500 | 账号认证 | server 内置认证模块 |
| DBServer | 5100/6000 | 角色CRUD | server 内置数据模块（SQLite） |
| M2Server | 5000 | 游戏逻辑 | server 核心（已有） |
| LogDataServer | UDP | 日志收集 | server 内置日志（使用 internal/log） |
| GameCenter | - | 管理控制台 | 不需要（CLI + 日志） |

### 单端口统一监听

server 监听一个端口（默认 7000），内部根据消息内容自动路由：
- 客户端连接后发送 `CM_IDPASSWORD` → 认证流程（原 LoginSrv 功能）
- 认证成功后发送 `CM_QUERYCHR` / `CM_SELCHR` → 角色管理（原 DBServer 功能）
- 选择角色后发送游戏消息 → 游戏逻辑（原 M2Server 功能）

客户端不需要切换连接，全流程在一个 TCP 连接上完成。

### SQLite 数据存储

取代原 Delphi 的平面文件存储（Mir.DB、MirChr.DB、ID.DB）：
- `server_data/accounts.db` — 账号表（原 LoginSrv 的 ID.DB）
- `server_data/characters.db` — 角色存档（原 DBServer 的 MirChr.DB）
- `server_data/guilds.db` — 行会数据（原 GuildBase/）

server.jsonc 中新增数据库配置：
```jsonc
{
  "database": {
    "accounts": "server_data/accounts.db",
    "characters": "server_data/characters.db",
    "guilds": "server_data/guilds.db"
  }
}
```

## 关键设计决策

1. **单端口统一监听** — server 监听一个端口（默认7000），内部自动路由登录/角色/游戏消息，客户端无需切换连接

2. **SQLite 数据存储** — 账号、角色、行会数据统一存储在 SQLite 数据库中，单文件便于备份和管理

3. **GBK 编码处理** — 原始文件为 GBK/GB2312 编码，转换时自动转为 UTF-8

4. **NPC 脚本保持原格式** — 不解析脚本语义，只做包装。后续由服务端脚本引擎直接读取原始格式

5. **空文件跳过** — 大量空文件（Deny*.txt、Disable*.txt 等）不生成对应的 JSONC，只记录到日志

6. **Map 文件直接复制** — 二进制 .map 文件不做转换，直接复制到 `server_config/maps/`

7. **SQLite 数据优先** — 物品、怪物、魔法定义以 SQLite 数据库为准（比配置文件更完整）

8. **配置简化** — 原 `!setup.txt` 中的多进程地址/端口配置（DBAddr、IDSAddr、GateAddr 等）全部移除，只保留游戏逻辑配置

## 验证方案

1. 运行 `go run ./cmd/serverconfig --input asset/server --output server_config`
2. 检查 `server_config/` 目录结构是否完整
3. 抽查关键文件：
   - `server.jsonc` 包含所有原始配置项（不含多进程地址配置）
   - `items/std_items.jsonc` 物品数量与 SQLite 一致
   - `monsters/monster_db.jsonc` 怪物数量与 SQLite 一致
   - `maps/map_info.jsonc` 地图数量与 mapinfo.txt 一致
   - `maps/*.map` 文件数量与原始 Map/ 目录一致
4. 验证 JSONC 格式合法性（可被 JSON 解析器正确解析）
5. 验证中文内容正确转换为 UTF-8
