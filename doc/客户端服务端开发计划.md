# 热血传奇 Go 重制版 — 客户端 & 服务端同步开发计划

## 设计理念

### 同步开发策略

本计划采用**客户端与服务端同步开发**的方式，每个阶段同时推进两端的工作。每个阶段结束时都有一个**可运行的集成里程碑**，确保两端始终保持兼容。

```
Phase 1: 基础层（共享协议 & 类型）
    ↓
Phase 2: 客户端骨架 + 服务端骨架（并行）
    ↓
Phase 3: 渲染层 + 世界层（并行）→ 里程碑：地图可视化
    ↓
Phase 4: 角色层 + 游戏逻辑层（并行）→ 里程碑：角色可见可移动
    ↓
Phase 5: 网络层 + 消息处理层（并行）→ 里程碑：端到端登录
    ↓
Phase 6: 战斗层 + 魔法层（并行）→ 里程碑：可战斗
    ↓
Phase 7: 社交层 + NPC层（并行）→ 里程碑：可交互
    ↓
Phase 8: 进阶系统 + 打磨（并行）→ 里程碑：生产就绪
```

### 现代架构设计原则

1. **接口驱动设计** — 核心系统通过接口交互，便于测试和替换
2. **消息总线架构** — 客户端和服务端内部使用 channel-based 消息总线
3. **组件化实体系统 (ECS)** — 使用组合模式而非深度继承（优于 Delphi 的 TBaseObject 继承链）
4. **依赖注入** — 核心服务通过构造函数注入，便于单元测试
5. **优雅关闭** — 所有 goroutine 通过 context 和 channel 协调关闭

## 现状评估

| 组件 | 状态 | 说明 |
|------|------|------|
| internal/mapformat | ✅ 完成 | .map 解析器，12/14/20字节单元格，列优先→行优先 |
| internal/wil | ✅ 完成 | .wil/.wix 加载器，标准+ILib格式，调色板→RGBA，底到顶行翻转 |
| internal/log | ✅ 完成 | 分级日志（TRACE→ERROR） |
| cmd/mapviewer | ✅ 完成 | OpenGL 3.3 + ImGui，三层渲染，小地图，相机 |
| cmd/wilviewer | ✅ 完成 | OpenGL 3.3 + ImGui，浏览/动画/导出 |
| cmd/client | ✅ Phase 5A 部分完成 | 客户端入口、场景状态机、网络层、角色系统、地图渲染、文字渲染(TextRenderer)、登录/选角/公告场景交互、三阶段断开重连登录流程 |
| cmd/server | ✅ Phase 5B 完成 | 服务端入口、会话管理、消息处理、游戏对象、世界管理 |

## 同步开发依赖图

```
Phase 1: internal/protocol（共享基础）
    │
    ├─── Phase 2A: cmd/client 骨架     ├─── Phase 2B: cmd/server 骨架
    │         │                              │
    │    Phase 3A: 客户端渲染层         Phase 3B: 服务端世界层
    │         │                              │
    │    Phase 4A: 客户端角色层         Phase 4B: 服务端游戏逻辑
    │         │                              │
    │    Phase 5A: 客户端网络层         Phase 5B: 服务端消息处理
    │         │                              │
    │         └──────── 里程碑5 ──────────────┘
    │                   (端到端登录)
    │
    ├─── Phase 6A: 客户端战斗/魔法     ├─── Phase 6B: 服务端战斗/魔法
    │         │                              │
    │         └──────── 里程碑6 ──────────────┘
    │                   (可战斗)
    │
    ├─── Phase 7A: 客户端UI系统        ├─── Phase 7B: 服务端NPC/脚本
    │         │                              │
    │         └──────── 里程碑7 ──────────────┘
    │                   (可交互)
    │
    └─── Phase 8A: 客户端打磨          ├─── Phase 8B: 服务端进阶
              │                              │
              └──────── 里程碑8 ──────────────┘
                        (生产就绪)
```

---

## Phase 1-5 完成情况与缺口分析

### 已完成模块概览

| 模块 | 状态 | 关键功能 |
|------|------|---------|
| internal/protocol | ✅ Phase 1 | 6Bit编解码、~300消息常量、所有数据结构(StdItem/UserItem/Ability/Magic) |
| internal/engine | ✅ Phase 2A | GLFW窗口、OpenGL状态、着色器、2D相机、场景状态机、WIL资源管理器 |
| internal/netserver | ✅ Phase 2B | TCP服务器、Session状态机(Connected→Authenticated→InGame)、读写协程 |
| internal/storage | ✅ Phase 2B | SQLite数据库、accounts/characters/guilds/character_items表 |
| internal/mapformat | ✅ | .map文件解析器(12/14/20字节)、列优先→行优先、碰撞检测 |
| internal/wil | ✅ | .wil/.wix图像加载器(8位调色板/16位RGB565)、ILib格式、底到顶行翻转 |
| cmd/serverconfig | ✅ | Delphi配置→JSONC转换工具(INI/SQLite/GBK自动检测) |
| cmd/server | ✅ Phase 5B | 地图加载、Environment对象管理、BaseObject/PlayObject、移动碰撞、消息路由 |
| cmd/client | ✅ Phase 5A 部分完成 | 三层地图渲染、HA+MA9-MA47动画模板、Actor实体、Y排序渲染、文字渲染(TextRenderer)、登录/选角/公告场景交互、三阶段断开重连登录流程 |

### 登录流程实现状态

当前已实现的端到端流程（三阶段断开重连）：

```
客户端                                    服务端
  │                                         │
  阶段1: 登录服务器 (port 7000)
  │                                         │
  ── CMProtocol(2000) ────────────────────────────────────▶│  协议版本
  ── CMIDPassword(2001) + "uid/passwd" ──────────────────▶│  登录凭据
  │                                         │
  ├── SMPassOKSelectServer(529) ──────────────────────────────────┘  服务器列表
  │     → 解析服务器列表，开门动画           │
  ── CMSelectServer(104) + "serverName" ──────────────────▶│  选择服务器
  │                                         │
  ├── SMSelectServerOK(530) ──────────────────────────────────────┘  "addr/port/cert"
  │     → 断开连接，重连到选角服务器         │
  │     → 存储 certification                │
  │                                         │
  阶段2: 选角服务器 (addr:port from SM_SELECTSERVER_OK)
  │                                         │
  ── CMProtocol(2000) ────────────────────────────────────▶│  协议版本
  ── CMQueryChr(100) + "loginId/cert" ────────────────────▶│  查询角色
  │                                         │
  ├── SMQueryChr(520) ────────────────────────────────────────────┘  角色列表
  │     → 解析角色列表，填充选角场景         │
  │     → 用户选择角色                      │
  ── CMSelChr(103) + "loginId/charName" ──────────────────▶│  选择角色
  │                                         │
  ├── SMStartPlay(525) ────────────────────────────────────────────┘  "runAddr/runPort"
  │     → 断开连接，重连到游戏服务器         │
  │                                         │
  阶段3: 游戏服务器 (addr:port from SM_STARTPLAY)
  │                                         │
  ── "**loginId/charName/cert/version/9" ─────────────────▶│  RunLogin认证
  │                                         │
  ├── SMSendNotice(658) ──────────────────────────────────────────┘  公告内容
  │     → 显示公告，等待用户确认             │
  ── CMLoginNoticeOK(1018) ───────────────────────────────▶│  确认公告
  │                                         │
  ├── SMNewMap(51) ───────────────────────────────────────────────┘  地图+坐标
  ├── SMLogon(50) ────────────────────────────────────────────────┘  玩家数据
  │                                         │
  │  [进入游戏场景]                          │
```

### 十大游戏系统缺口分析

#### 1. 战斗系统 — 完全缺失

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 伤害计算 | ❌ 缺失 | 无攻防公式、无伤害计算 |
| 命中检测 | ❌ 缺失 | 攻击不查找目标、不检测范围 |
| HP/MP管理 | 🔶 骨架 | Ability.HP/MP 字段存在，无扣减/恢复逻辑 |
| 死亡处理 | ❌ 缺失 | 无死亡判定、经验掉落、重生逻辑 |
| PK系统 | ❌ 缺失 | HAMAll/HAMPeace 常量存在，无实现 |
| 毒/Debuff | ❌ 缺失 | PoisonDecHealth 等常量存在，无实现 |
| 被击反馈 | ❌ 缺失 | SMStruck/ActStruck 存在，无处理 |
| 特殊攻击 | ❌ 缺失 | CMHeavyHit/CMBigHit 等6种类型，HandleHit 不区分 |

协议就绪：~20个战斗消息ID已定义。数据结构就绪：Ability(50B)含AC/MAC/DC/MC/SC全套攻防属性。

#### 2. 怪物AI — 完全缺失

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 怪物实体 | ❌ 缺失 | 无 MonsterObject 类型 |
| 生成系统 | ❌ 缺失 | 无刷怪配置、无定时刷新 |
| 行为AI | ❌ 缺失 | 无 idle/chase/attack/return 状态机 |
| 寻路 | ❌ 缺失 | CanWalk 只做单格碰撞，无A* |
| 仇恨系统 | ❌ 缺失 | 无目标选择、无仇恨列表 |
| 客户端渲染 | ✅ 完整 | MA9-MA47模板 + GetMonOffset + ActorManager |

客户端渲染框架完备，等待服务端发送怪物数据。

#### 3. 物品系统 — 骨架存在，逻辑缺失

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 背包管理 | 🔶 骨架 | ItemList[46] 存在，无添加/移除/排序 |
| 穿戴/卸下 | ❌ 缺失 | UseItems[13] 存在，CMTakeOnItem 无处理 |
| 物品掉落 | ❌ 缺失 | DropItem 结构体存在，无掉落表 |
| 拾取 | ❌ 缺失 | CMPickup 存在，无处理 |
| 耐久度 | ❌ 缺失 | Dura/DuraMax 字段存在，无消耗逻辑 |
| 物品使用 | ❌ 缺失 | CMEat 存在，无实现 |
| 物品定义加载 | ❌ 缺失 | StdItem 完整(60B)，serverconfig 有数据，服务端不加载 |
| 装备属性计算 | ❌ 缺失 | AddAbility 结构体存在，无加成计算 |
| 数据库持久化 | 🔶 骨架 | character_items 表已建，无读写操作 |

协议就绪：~30个物品消息ID。数据结构就绪：StdItem(60B), UserItem(24B), AddAbility。

#### 4. UI系统 — 完全缺失

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 血条 | ❌ 缺失 | SMOpenHealth 存在，无渲染 |
| 背包窗口 | ❌ 缺失 | 无UI框架 |
| 聊天窗口 | ❌ 缺失 | 无输入框、无消息区 |
| 名字标签 | ❌ 缺失 | Actor.UserName 存在，无文字渲染 |
| 伤害数字 | ❌ 缺失 | 无浮动数字系统 |
| 角色面板 | ❌ 缺失 | SMAbility 存在，无显示 |
| 小地图 | 🔶 代码完成未集成 | minimap.go 完整，scene_play 未调用 |
| 登录/选角界面 | ✅ 完成 | LoginScene(输入+按钮+开门动画)、SelectChrScene(角色选择)、NoticeScene(公告确认) |
| 文字渲染 | ✅ 完成 | TextRenderer(opentype TTF)、支持中英文、字形纹理缓存、DrawQuadTint颜色调制 |

关键瓶颈：客户端无UI框架（ImGui或自建），所有UI功能的前置依赖。

#### 5. NPC/脚本系统 — 完全缺失

| 子系统 | 状态 | 说明 |
|--------|------|------|
| NPC对话 | ❌ 缺失 | CMClickNPC/SMMerchantSay 存在，无处理 |
| 任务系统 | ❌ 缺失 | QuestUnit[128] 字段存在，无引擎 |
| 商店 | ❌ 缺失 | 20+商店消息存在，无商人逻辑 |
| 修理 | ❌ 缺失 | CMUserRepairItem 存在，无实现 |
| NPC脚本引擎 | ❌ 缺失 | 无解析器、无条件-动作系统 |
| NPC渲染 | ✅ 完整 | getNPCBodyImage + GetNpcOffset |

协议就绪：~40个NPC/商人/任务消息ID。配置就绪：serverconfig/npcs/ 有转换后的NPC数据。

#### 6. 社交系统 — 完全缺失

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 聊天 | ❌ 缺失 | CMSay/SMHear/SMCry/SMWhisper 存在 |
| 组队 | ❌ 缺失 | 10+组队消息存在 |
| 行会 | ❌ 缺失 | 20+行会消息存在，DB有 guilds 表 |
| 交易 | ❌ 缺失 | 15+交易消息存在 |
| 广播 | ❌ 缺失 | 玩家移动不广播给其他人 |

协议就绪：~60个社交消息ID。

#### 7. 成长系统 — 骨架存在

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 升级 | ❌ 缺失 | Level/Exp/MaxExp 字段存在，无逻辑 |
| 属性分配 | ❌ 缺失 | NakedAbility 结构体存在 |
| 技能学习 | ❌ 缺失 | UserMagic 完整，MagicList 存在 |
| 技能释放 | ❌ 缺失 | HandleSpell 只打日志 |
| 经验表 | ❌ 缺失 | Exps.ini 存在，服务端不加载 |
| 职业差异 | ❌ 缺失 | Job 字段存在，无职业逻辑 |

#### 8. 地图高级功能 — 半成品

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 地图加载 | ✅ 已完成 | MapManager.LoadAllMaps() |
| 碰撞检测 | ✅ 已完成 | envir.CanWalk() |
| 传送 | 🔶 骨架 | MapRoute + FindRoute 存在，无触发 |
| 安全区 | ❌ 缺失 | MapFlag.Safe 字段存在，永远 false |
| 地图事件 | ❌ 缺失 | OS_MAPEVENT 常量存在，无事件系统 |
| 门系统 | 🔶 半成品 | ProcessDoors 有超时逻辑，不改变碰撞 |
| 地图切换 | ❌ 缺失 | SMChangeMap 存在，客户端只加载一张图 |
| 视野广播 | ❌ 缺失 | GetRangeObjects 存在，未被使用 |

#### 9. 音频 — 完全缺失

无音频库依赖、无音效/BGM加载管线。ChrMsg.Sound 字段存在但未使用。

#### 10. 安全 — 几乎为零

| 子系统 | 状态 | 说明 |
|--------|------|------|
| 账号验证 | ❌ 缺失 | CMIDPassword 直接通过，不查DB |
| 密码哈希 | 🔶 骨架 | DB有 password_hash 字段，不验证 |
| 反作弊 | ❌ 缺失 | 不验证坐标/方向/攻击频率 |
| 速率限制 | ❌ 缺失 | 无消息频率限制 |
| 输入验证 | ❌ 缺失 | HandleRun 连续两次 WalkTo 不检查第一次是否成功 |

### 优先级路线图（更新）

#### P0 — 核心循环（没有就没有游戏）

1. **接入UserEngine到游戏循环** — tick -> ProcessHumans -> Operate
2. **登录流程补全** — DB验证密码、加载角色列表、创建PlayObject、从DB读取坐标
3. **广播系统** — 玩家移动/动作同步给视野内其他玩家
4. **HP/MP管理** — 伤害扣血、死亡判定、重生
5. **基础战斗** — HandleHit 实现、近身攻击、伤害公式
6. **物品穿戴/背包** — CMTakeOnItem/TakeOffItem、装备属性加成到 WAbil
7. **客户端UI框架** — 集成ImGui或自建（血条、背包窗口、名字标签）

#### P1 — 可玩内容（有了才有东西玩）

8. **怪物生成+基础AI** — MonsterObject、刷怪配置、idle/chase/attack
9. **经验/升级** — 杀怪经验、升级属性提升
10. **物品掉落/拾取** — 怪物掉落表、地面物品、CMPickup
11. **NPC对话+商店** — 基础对话、买卖物品
12. **技能释放** — HandleSpell、基础魔法效果
13. **聊天系统** — CMSay 处理、SMHear 广播

#### P2 — 完整体验

14. 物品耐久/修理
15. 任务系统基础
16. 传送/地图切换
17. 组队系统
18. 毒/Debuff
19. 安全加固（反作弊/速率限制）

#### P3 — 社交与进阶

20. 行会系统
21. 交易系统
22. PK系统
23. 城堡战
24. 音频

---

## Phase 1: 基础层 — 共享数据 & 协议

**目标**: 将 Delphi Common/ 目录的所有类型和6Bit协议移植为可复用的Go包

**复杂度**: M | **预计**: 1-2周

### 任务

#### 1.1 internal/protocol/edcode.go — 6Bit 编解码

移植 Common/EDcode.pas：

- Encode6BitBuf(src []byte) []byte — 3字节→4字符，偏移0x3C
- Decode6BitBuf(src []byte) []byte — 反向
- EncodeMessage(msg DefaultMessage) string / DecodeMessage(s string) DefaultMessage
- EncodeString(s string) string / DecodeString(s string) string
- EncodeBuffer(buf []byte) string / DecodeBuffer(s string, buf []byte)
- 可选 XOR 掩码表 (EncodeBitMasks[256], DecodeBitMasks[256])

#### 1.2 internal/protocol/message.go — 消息类型

移植 Common/Grobal2.pas 消息定义：

- DefaultMessage 结构体: Recog int32, Ident uint16, Param uint16, Tag uint16, Series uint16（16字节，packed）
- MakeDefaultMsg(ident uint16, recog int32, param, tag, series uint16) DefaultMessage
- 所有 CM_* 常量（客户端→服务端，约50个ID）
- 所有 SM_* 常量（服务端→客户端，约100个ID）
- 消息帧格式: '#' + code(1位数字) + 编码数据 + '!'

#### 1.3 internal/protocol/types.go — 共享数据结构

移植 Common/Grobal2.pas 类型：

- 方向常量: DR_UP(0) ~ DR_UPLEFT(7)
- 网格常量: UNITX=48, UNITY=32, LOGICALMAPUNIT=40
- 装备槽常量: U_DRESS(0) ~ U_BUJUK(9)（10个槽位）
- StdItem 结构体（60字节）
- UserItem 结构体（24字节）
- Ability 结构体（50字节）
- Magic 结构体
- MsgHeader 结构体
- 毒类型、攻击模式、物品类型等常量

#### 1.4 internal/protocol/types_test.go — 编码测试

- 往返测试: EncodeMessage → DecodeMessage
- 往返测试: EncodeBuffer → DecodeBuffer（各种大小）
- 验证结构体大小

### 交付物

protocol 包能编解码所有 MIR2 网络消息。通过基于已知 Delphi 编码字节序列的单元测试验证。

---

## Phase 2A: 客户端 — 窗口 & 场景框架

**目标**: 创建客户端应用壳，包含GLFW窗口、OpenGL上下文、ImGui集成、场景状态机和WIL资源管理器

**复杂度**: M | **预计**: 1-2周（与 Phase 2B 并行）

### 任务

#### 2A.1 internal/engine/window.go — GLFW 窗口

- Window 结构体：封装GLFW窗口创建（OpenGL 3.3 Core Profile）
- 可配置大小（默认1024×768）、标题、全屏切换
- 输入回调注册（键盘、鼠标、滚轮）
- 主循环抽象: Run(updateFn, renderFn)

#### 2A.2 internal/engine/gl.go — OpenGL 状态

- GLState 结构体: 单元四边形VAO/VBO、白色纹理、着色器程序
- UploadTexture(rgba *image.RGBA) uint32
- DeleteTexture(id uint32)
- DrawQuad(texID uint32, x, y, w, h float32, model, proj mgl32.Mat4)
- OrthoProj(width, height float32) mgl32.Mat4

#### 2A.3 internal/engine/shader.go — 着色器程序

- MainShader — 纹理四边形着色器
- ColorShader — 纯色着色器

#### 2A.4 internal/engine/resourcemanager.go — WIL 资源缓存

- ResourceManager 结构体：持有所有已加载WIL文件
- Load(dataDir string) error — 从 asset/client/Data/ 加载所有WIL文件
- 怪物文件延迟加载（Mon1~Mon28）
- LRU淘汰策略

#### 2A.5 internal/engine/scene.go — 场景状态机

- SceneType 枚举: SceneIntro, SceneLogin, SceneSelectChr, SceneLoginNotice, ScenePlayGame
- Scene 接口: Open(), Close(), Update(dt float64), Render(), OnKey(), OnMouse(), OnScroll()
- SceneManager 结构体

#### 2A.6 cmd/client/main.go — 客户端入口

- 解析CLI参数（数据目录、服务器地址）
- 创建窗口、GL状态、资源管理器
- 初始化场景管理器
- 主循环

### 交付物

客户端窗口打开后显示ImGui调试覆盖层，WIL资源加载成功。

---

## Phase 2B: 服务端 — 核心基础设施

**目标**: 构建服务端核心：网络引擎、会话管理、数据存储和主循环

**复杂度**: L | **预计**: 2-3周（与 Phase 2A 并行）

### 任务

#### 2B.1 internal/netserver/server.go — TCP 服务器

- TCPServer 结构体: 监听器, 接受循环, 连接管理
- Listen(addr string) error
- 每连接一个goroutine: 读循环, 缓冲管理, 消息帧
- OnConnect, OnDisconnect, OnMessage 回调

#### 2B.2 internal/storage/sqlite.go — SQLite 存储层

- Database 结构体: 封装 sql.DB
- Open(path string) error
- 账号表: accounts(id, username, password_hash, created_at)
- 角色表: characters(id, account_id, name, job, sex, level, map, x, y, ...)
- 行会表: guilds(id, name, leader_id, ...)
- 物品表: character_items(character_id, slot_type, slot_index, item_data)

#### 2B.3 cmd/server/session.go — 会话管理

- Session 结构体: ID, AccountName, State, Player, Conn
- SessionState 枚举: Connected, Authenticated, CharacterSelected, InGame
- SessionManager 结构体

#### 2B.4 cmd/server/config.go — 服务端配置

- ServerConfig 结构体
- LoadConfig(filename string) error

#### 2B.5 cmd/server/main.go — 服务端入口

- 初始化: Database, SessionManager, TCPServer
- 启动TCP服务器监听（默认端口7000）
- 主tick循环: 100ms间隔（10 ticks/秒）
- SIGINT/SIGTERM 优雅关闭

### 交付物

服务端启动，监听端口7000，接受客户端连接，管理会话状态。

---

## Phase 3A: 客户端 — 游戏场景 & 地图渲染

**目标**: 使用三层瓦片系统渲染游戏世界，支持相机和坐标变换

**复杂度**: L | **预计**: 2-3周（与 Phase 3B 并行）

### 任务

#### 3A.1 internal/engine/camera.go — 2D 相机

- Camera 结构体: X, Y float64, Zoom float32
- ScreenToWorld, WorldToTile, TileToScreen 坐标变换
- ViewportTiles() — 可见瓦片范围
- CenterOn, Pan, ZoomAt, ClampToBounds

#### 3A.2 cmd/client/scene_play.go — 游戏场景（地图渲染）

- PlayScene 结构体
- 从本地 `asset/client/Map/` 加载 .map 文件
- DrawTileMap() — 三层渲染:
  - 背景层 (Tiles.wil): stride-2, 2×2块
  - 中间层 (SmTiles.wil): 逐格, 透明
  - 前景层 (Objects.wil): 逐格, 动画帧循环, 混合模式
- 区域系统: 根据 Area 字段延迟加载 Objects{N+1}.wil
- 坐标变换和视口裁剪

#### 3A.3 cmd/client/minimap.go — 小地图

- 碰撞纹理生成
- 视口矩形覆盖层
- 点击导航支持

### 交付物

客户端渲染完整MIR2地图，支持相机平移/缩放，显示小地图。

---

## Phase 3B: 服务端 — 地图 & 世界管理

**目标**: 服务端地图加载、空间查询和对象放置

**复杂度**: L | **预计**: 2周（与 Phase 3A 并行）

### 任务

#### 3B.1 cmd/server/envir.go — 地图环境

- MapCellInfo 结构体: Flag byte, ObjList []OSObject
- Environment 结构体: Header, MapCellArray, Flag, DoorList
- LoadMapData(filename string) error
- AddToMap, DeleteFromMap, MoveToMovingObject
- CanWalk, CanWalkEx, CanFly
- GetMovingObject, GetRangeBaseObject, GetItem

#### 3B.2 cmd/server/mapmanager.go — 地图管理器

- MapManager 结构体: maps map[string]*Environment
- LoadAllMaps() — 启动时加载所有 .map 文件
- FindMap(name string) *Environment
- 从 asset/server/Map/ 加载所有地图

#### 3B.3 cmd/server/doors.go — 门系统

- Door 结构体: ID, Position, State, OpenTick
- ProcessMapDoors() — 超时后自动关门

### 交付物

服务端加载所有地图，支持空间查询，处理门状态转换。

### 里程碑3：地图可视化

客户端和服务端各自加载本地 .map 文件，地图数据一致。

---

## Phase 4A: 客户端 — 角色系统

**目标**: 实现可视化角色系统（玩家、怪物、NPC）及动画

**复杂度**: XL | **预计**: 3-4周（与 Phase 4B 并行）

### 任务

#### 4A.1 cmd/client/actor.go — 角色基类 & 动画模板

- ActionInfo 结构体: Start, Frame, Skip, FTime, UseTick
- HumanAction — 14个动作
- MonsterAction — 7个动作
- HumanActions 模板 — 与Delphi HA 完全一致
- MonsterActions 模板 — MA9 ~ MA47（39个模板）
- 帧计算公式
- WORDER 数组 — 武器覆盖层排序

#### 4A.2 cmd/client/actor.go — Actor 基础结构体

- 身份: RecogID, UserName
- 位置: CurrX, CurrY, Dir, ShiftX, ShiftY
- 外观: Sex, Race, Hair, Dress, Weapon, Job, Appearance
- 状态: Death, Skeleton, WarMode
- 渲染: BodySurface, BodyOffset, HairOffset, WeaponOffset
- 动画: StartFrame, EndFrame, CurrentFrame, FrameTime

#### 4A.3 cmd/client/actor_hum.go — THumActor

- 附加表面: HairSurface, WeaponSurface, HumWinSurface
- LoadSurface() — 多层加载
- DrawChr() — 组合渲染
- HUMANFRAME = 600

#### 4A.4 cmd/client/actor_npc.go — TNpcActor

- 从 Npc.wil 加载（每NPC 60帧）

#### 4A.5 cmd/client/actor_monster.go — TMonsterActor

- 根据外观从 Mon{N}.wil 加载
- MONFRAME=280, EXPMONFRAME=360, SCULMONFRAME=440

#### 4A.6 cmd/client/actor_manager.go — 角色生命周期

- ActorManager 结构体: actors map[int32]*Actor
- FindActor, NewActor, DeleteActor, ClearActors
- Y轴排序渲染

#### 4A.7 cmd/client/animation.go — 动画播放器

- AnimationState 结构体
- Update(now int64) bool
- SetAction, Reset

### 交付物

角色在地图上正确渲染和动画。

---

## Phase 4B: 服务端 — 游戏对象 & 玩家逻辑

**目标**: 实现服务端游戏对象系统、玩家连接生命周期、属性、装备和背包

**复杂度**: XL | **预计**: 3-4周（与 Phase 4A 并行）

### 任务

#### 4B.1 internal/game/entity.go — 实体组件系统（ECS）

采用组件化设计替代深度继承：

- Entity 结构体: ID int32, Components map[ComponentType]Component
- Component 接口: Type() ComponentType, Update(dt float64)
- 组件类型:
  - TransformComponent: X, Y, Direction
  - HealthComponent: HP, MaxHP, MP, MaxMP
  - CombatComponent: AC, MAC, DC, MC, SC, HitPoint, Speed
  - InventoryComponent: Items, Equipment
  - MovementComponent: Speed, WalkSpeed, RunSpeed
  - AnimationComponent: CurrentAction, Frame, Direction
  - AIComponent: AIType, Target, State

#### 4B.2 cmd/server/baseobject.go — TBaseObject（兼容层）

基于 Entity 组件系统实现兼容接口：

- 身份、属性、战斗、状态、位置、社交、召唤
- 装备: UseItems [13]*UserItem, ItemList []*UserItem
- 魔法: MagicList []*UserMagic
- 可见性: VisibleActors, VisibleItems, VisibleEvents
- 消息: MsgList []SendMessage（线程安全队列）
- 核心方法: SendMsg, WalkTo, TurnTo, Attack, EnterAnotherMap

#### 4B.3 cmd/server/playobject.go — TPlayObject

- 连接: GateIdx, Socket
- 会话: AccountName, SessionID
- Operate() — 每tick主处理
- ClientMsg(msg DefaultMessage, body string) bool

#### 4B.4 cmd/server/inventory.go — 背包系统

- AddItem, DeleteItem, FindItem
- TakeOnEquipment, TakeOffEquipment, UseItem
- 重量计算

#### 4B.5 cmd/server/itemdef.go — 物品定义

- StdItemDef 结构体
- LoadStdItems, GetStandardItem
- RandomUpgradeItem — 随机属性加成

#### 4B.6 cmd/server/auth.go — 认证模块

- 处理 CM_IDPASSWORD，验证账号/密码
- 会话管理：SessionID 生成、验证、超时

#### 4B.7 cmd/server/charmgr.go — 角色管理模块

- 处理 CM_QUERYCHR, CM_NEWCHR, CM_DELCHR, CM_SELCHR
- 查询 SQLite characters 表

### 交付物

服务端可从SQLite加载角色数据，处理基本客户端消息。

### 里程碑4：角色可见可移动

客户端可以显示服务端管理的角色，玩家可以控制角色在地图上移动。

---

## 客户端场景系统（严格复刻 Delphi）

### 场景定义

| 场景 | 类型 | 说明 |
|------|------|------|
| stIntro(0) | TIntroScene | 空实现 |
| stLogin(1) | TLoginScene | 登录场景：背景、ID/密码输入 |
| stSelectCountry(2) | — | 未使用 |
| stSelectChr(3) | TSelectChrScene | 角色选择：2个角色槽、冰冻/解冻动画 |
| stNewChr(4) | — | 未使用 |
| stLoading(5) | — | 未使用 |
| stLoginNotice(6) | TLoginNotice | 登录公告：公告文本、确认按钮 |
| stPlayGame(7) | TPlayScene | 游戏场景：3层地图、角色、UI |

### 场景切换流程

`
客户端启动
    │
    └─→ SceneLogin（登录场景）
            │
            │  用户按 F9 连接服务器
            │
            ├─→ CM_IDPASSWORD ──────────────────────→ 服务端
            │                                         │
            │                                         │ 验证账号密码
            │                                         │
            │←── SM_PASSOK_SELECTSERVER ─────────────┘
            │
            │  [服务器选择：自动完成]
            ├─→ CM_SELECTSERVER ─────────────────────→ 服务端
            │                                         │
            │←── SM_SELECTSERVER_OK ─────────────────┘
            │
            └─→ SceneSelectChr（角色选择场景）
                    │
                    ├─→ CM_QUERYCHR ─────────────────→ 服务端
                    │                                   │
                    │←── SM_QUERYCHR ─────────────────┘
                    │
                    │  [用户选择角色]
                    ├─→ CM_SELCHR ───────────────────→ 服务端
                    │                                   │
                    │←── SM_STARTPLAY ────────────────┘
                    │
                    └─→ SceneLoginNotice（公告场景）
                            │
                            │←── SM_SENDNOTICE ────────── 服务端
                            │
                            │  [显示公告，自动确认]
                            ├─→ CM_LOGINNOTICEOK ─────→ 服务端
                            │
                            │←── SM_NEWMAP ───────────── 服务端
                            │←── SM_LOGON ────────────── 服务端
                            │
                            └─→ ScenePlayGame（游戏场景）
                                    │
                                    ├─→ CM_QUERYBAGITEMS ─→ 服务端
                                    │
                                    │←── SM_BAGITEMS ────── 服务端
                                    │←── SM_ABILITY ─────── 服务端
                                    │
                                    └─→ [游戏进行中]
`

### 与 Delphi 原版的差异

| 项目 | Delphi 原版 | Go 实现 |
|------|------------|---------|
| TCP 连接 | 3个（LoginSrv:7000, SelGate:7100, RunGate:7200） | 1个（:7000） |
| 服务器选择 | 弹出服务器列表让用户选择 | 自动选择（简化） |
| 角色创建 | 独立场景 stNewChr | 未实现 |
| 登录公告 | 显示公告文本，用户点击确认 | 自动确认（简化） |
| 角色选择动画 | 冰冻/解冻动画（13帧+14帧特效） | 占位渲染 |

---

## Phase 5A: 客户端 — 网络层 & 输入

**目标**: 实现客户端TCP网络通信和玩家输入处理

**复杂度**: L | **预计**: 2周（与 Phase 5B 并行）

### 任务

#### 5A.1 internal/netclient/client.go — TCP 客户端

- Client 结构体: conn net.Conn, 缓冲区, 互斥锁
- Connect(addr string) error
- Send(msg DefaultMessage, extra string)
- 读循环（goroutine）: 提取消息帧，分发到处理器
- 指数退避重连逻辑

#### 5A.2 cmd/client/net_handler.go — 客户端消息处理器

- 登录流程处理器: SM_CERTIFICATION_SUCCESS, SM_QUERYCHR, SM_STARTPLAY 等
- 游戏处理器: SM_NEWMAP, SM_TURN, SM_WALK, SM_HIT, SM_HEAR 等

#### 5A.3 cmd/client/net_send.go — 客户端消息发送器

- SendLogin, SendNewChr, SendDelChr, SendSelChr
- SendSay, SendWalk, SendRun, SendHit, SendSpell
- SendPickup, SendDropItem, SendTakeOnItem, SendEat

#### 5A.4 cmd/client/input.go — 输入处理器

- 鼠标: 左键走/攻击, 右键跑, 中键拖拽
- 键盘: F1-F8魔法, 1-6物品, B/C/E/G/S/W面板

#### 5A.5 cmd/client/action_queue.go — 动作队列

- 动作锁定: 必须等待服务器响应
- 超时处理: 3秒后自动解锁

### 交付物

客户端可连接服务器，发送登录凭据，玩家可点击移动。

---

## Phase 5B: 服务端 — 消息处理 & 游戏循环

**目标**: 实现服务端消息路由、游戏主循环和玩家同步

**复杂度**: L | **预计**: 2-3周（与 Phase 5A 并行）

### 任务

#### 5B.1 cmd/server/usrengine.go — 用户引擎

- UserEngine 结构体: PlayObjectList, MonsterList, MagicList 等
- 处理方法: ProcessHumans, ProcessMonsters, ProcessMerchants, ProcessNpcs
- 查询方法: GetStdItem, GetPlayObject, FindMagic

#### 5B.2 cmd/server/runsocket.go — RunSocket（网络引擎）

- RunSocket 结构体: 连接数组, 互斥锁
- AddGate, ExecGateBuffers, ExecGateMsg
- DoClientCertification — 客户端认证握手

#### 5B.3 cmd/server/frnengine.go — 前端引擎（DB I/O）

- FrontEngine 结构体: 加载队列, 保存队列
- Start() — 启动goroutine进行DB I/O
- LoadHumFromDB, SaveHumRcdToDB

#### 5B.4 cmd/server/main.go — 完整服务端入口

- 初始化所有组件
- 主tick循环: 100ms间隔（10 ticks/秒）
- SIGINT/SIGTERM 优雅关闭

### 交付物

服务端以10 ticks/秒运行主游戏循环。

### 里程碑5：端到端登录

完整登录流程：客户端连接 → 认证 → 角色列表 → 选择角色 → 进入游戏 → 移动。

---

## Phase 6A: 客户端 — 战斗 & 魔法视觉

**目标**: 实现战斗和魔法的客户端视觉效果

**复杂度**: L | **预计**: 2-3周（与 Phase 6B 并行）

### 任务

#### 6A.1 cmd/client/magic_effect.go — 魔法特效

- 投射物: 火球、符咒、飞斧
- 地面效果: 火墙、毒云、神圣屏障
- 爆炸效果: 雷电、冰咆哮

#### 6A.2 cmd/client/combat_visual.go — 战斗视觉

- 攻击动画、受击效果、死亡动画
- 伤害数字飘字、HP条显示

#### 6A.3 cmd/client/lighting.go — 光照系统

- 6级光罩加载
- LightMap, FogScreen
- 黑暗地图支持

### 交付物

战斗动作有正确视觉效果，魔法释放有特效，黑暗地图有光照效果。

---

## Phase 6B: 服务端 — 怪物AI & 战斗系统

**目标**: 实现怪物系统和战斗结算

**复杂度**: XL | **预计**: 3-4周（与 Phase 6A 并行）

### 任务

#### 6B.1 cmd/server/monster.go — 怪物基类

- Monster 结构体扩展BaseObject
- MonsterInfo 结构体
- Operate() — 每tick AI处理
- AttackTarget, FindTarget, GotoTarget

#### 6B.2 cmd/server/monster_types.go — 怪物变体

- 20+个不同行为的怪物类
- FastMonster, RangedMonster, StationaryMonster 等

#### 6B.3 cmd/server/combat.go — 战斗系统

- HitDamage, MagDamage — 伤害计算
- 伤害公式: DC - target.AC + 随机方差
- PK系统: PKPoint, PKFlag, PKTick
- 死亡处理: 掉落物品、金币

#### 6B.4 cmd/server/spawning.go — 怪物重生

- SpawnMonsters, RegenMonsters
- 刷新点配置来自 MonGen.txt

#### 6B.5 cmd/server/magic.go — 魔法管理器

- DoSpell — 主法术分发
- 战士/法师/道士技能各8+种

#### 6B.6 cmd/server/spell_effects.go — 法术效果

- 火墙、毒、神圣屏障、召唤

### 交付物

怪物刷新、追逐、攻击。战斗按正确伤害公式结算。

### 里程碑6：可战斗

玩家可以击杀怪物，使用各职业技能，获得经验值。

---

## Phase 7A: 客户端 — UI系统

**目标**: 实现完整的游戏UI

**复杂度**: L | **预计**: 2-3周（与 Phase 7B 并行）

### 任务

#### 7A.1 cmd/client/ui_manager.go — UI管理器

- ImGui集成: 使用 cimgui-go 绑定
- 窗口层级和焦点管理

#### 7A.2 cmd/client/ui_panels.go — 游戏面板

- DStateWin — 角色状态面板
- DItemBag — 背包面板（46格）
- DBottom — 底部快捷栏
- DMagicBook — 技能面板
- DMiniMap — 小地图面板

#### 7A.3 cmd/client/ui_npc.go — NPC交互UI

- DMerchantDlg — NPC对话窗口
- DShopDlg — 商店窗口
- DDealDlg — 交易窗口
- DStorageDlg — 仓库窗口

#### 7A.4 cmd/client/ui_social.go — 社交UI

- DGroupDlg — 组队窗口
- DGuildDlg — 行会窗口
- DChatBox — 聊天输入框

#### 7A.5 cmd/client/ui_login.go — 登录UI

- DLogIn — 登录窗口
- DSelectChr — 角色选择窗口
- DCreateChr — 角色创建窗口

### 交付物

完整的游戏UI系统。

---

## Phase 7B: 服务端 — NPC & 脚本系统

**目标**: 实现NPC系统，包含商店和任务脚本

**复杂度**: XL | **预计**: 3-4周（与 Phase 7A 并行）

### 任务

#### 7B.1 cmd/server/npc.go — NPC 基类

- NormNpc 结构体
- 脚本引擎: ScriptRecord, ScriptProcedure
- Click, UserSelect, GotoLabel

#### 7B.2 cmd/server/npc_script.go — 脚本引擎

- 条件评估器（60+条件）
- 动作执行器（80+动作）
- 基于标签的流程

#### 7B.3 cmd/server/merchant.go — 商人NPC

- GoodsList, RefillGoodsList
- ClientBuyItem, ClientSellItem, ClientRepairItem
- 仓库系统

#### 7B.4 cmd/server/npc_guild.go — 行会NPC

- GuildOfficial, CastleOfficial, Trainer

#### 7B.5 社交系统实现

- 聊天、组队、行会、交易系统

### 交付物

NPC交互、商店、任务脚本、社交系统。

### 里程碑7：可交互

玩家可以与NPC交互、购物、完成任务，与其他玩家聊天、组队、交易。

---

## Phase 8A: 客户端 — 打磨 & 音效

**目标**: 音效、性能优化、错误处理和调试工具

**复杂度**: M | **预计**: 2-3周（与 Phase 8B 并行）

### 任务

#### 8A.1 cmd/client/sound.go — 音效系统

- SoundManager 结构体
- 音效: 脚步声、攻击声、法术声、UI点击声
- 每地图背景音乐

#### 8A.2 cmd/client/performance.go — 性能优化

- 纹理图集、批量渲染、空间裁剪
- 帧率限制

#### 8A.3 cmd/client/debug.go — 调试工具

- FPS计数器、连接状态指示器
- ImGui调试覆盖层（F12切换）

#### 8A.4 cmd/client/error_handling.go — 错误处理

- 断线重连、错误消息、网络超时处理

### 交付物

生产就绪的客户端。

---

## Phase 8B: 服务端 — 进阶系统 & 优化

**目标**: 攻城战、武器升级、仓库、召唤、事件、性能优化

**复杂度**: L | **预计**: 2-3周（与 Phase 8A 并行）

### 任务

#### 8B.1 城堡系统

- UserCastle 结构体
- 攻城战: 定时攻击、旗帜占领

#### 8B.2 武器升级系统

- 升级成功时随机属性提升
- 失败时销毁武器

#### 8B.3 物品仓库

- 每玩家仓库（50格）

#### 8B.4 随机物品系统

- RandomUpgradeItem, RandomUpgradeUnknownItem

#### 8B.5 召唤系统

- MakeSlave — 创建召唤怪物
- 召唤物跟随主人

#### 8B.6 地图事件

- 火焰、毒云、神圣屏障、陷阱

#### 8B.7 管理员命令

- @GOTO, @RECALL, @KICK, @MAKE, @LEVEL, @MOB

#### 8B.8 性能优化

- 空间索引、消息批处理、Goroutine池
- pprof 性能分析

### 交付物

生产就绪的服务端。

### 里程碑8：生产就绪

完整的游戏，包含音效、配置、管理工具和优化性能。

---

## 最终包结构

```
mirgo/
├── go.mod
├── cmd/
│   ├── client/              # 游戏客户端
│   │   ├── main.go
│   │   ├── scene_*.go       # 场景实现
│   │   ├── actor*.go        # 角色系统
│   │   ├── input.go         # 输入处理
│   │   ├── net_*.go         # 网络处理
│   │   ├── ui_*.go          # UI系统
│   │   └── ...
│   ├── server/              # 游戏服务端
│   │   ├── main.go
│   │   ├── config.go        # 配置
│   │   ├── auth.go          # 认证
│   │   ├── charmgr.go       # 角色管理
│   │   ├── session.go       # 会话
│   │   ├── runsocket.go     # 网络引擎
│   │   ├── frnengine.go     # DB I/O
│   │   ├── usrengine.go     # 世界引擎
│   │   ├── baseobject.go    # 基础对象
│   │   ├── playobject.go    # 玩家对象
│   │   ├── envir.go         # 地图环境
│   │   ├── monster*.go      # 怪物系统
│   │   ├── combat.go        # 战斗系统
│   │   ├── magic.go         # 魔法系统
│   │   ├── npc*.go          # NPC系统
│   │   └── ...
│   ├── mapviewer/           # (已存在)
│   └── wilviewer/           # (已存在)
├── internal/
│   ├── log/                 # (已存在)
│   ├── mapformat/           # (已存在)
│   ├── wil/                 # (已存在)
│   ├── protocol/            # 新增: 共享协议
│   ├── engine/              # 新增: 共享渲染引擎
│   ├── netclient/           # 新增: TCP客户端库
│   ├── netserver/           # 新增: TCP服务端库
│   ├── game/                # 新增: 共享游戏逻辑（ECS）
│   └── storage/             # 新增: 数据存储层
├── serverconfig/            # JSONC配置文件
├── serverdata/              # SQLite数据库
└── asset/                   # (已gitignore)
```

---

## 复杂度汇总

| 阶段 | 名称 | 复杂度 | 预计周期 | 并行阶段 |
|------|------|--------|----------|----------|
| Phase 1 | 基础层 — 共享数据 & 协议 | M | 1-2周 | — |
| Phase 2A | 客户端 — 窗口 & 场景框架 | M | 1-2周 | Phase 2B |
| Phase 2B | 服务端 — 核心基础设施 | L | 2-3周 | Phase 2A |
| Phase 3A | 客户端 — 游戏场景 & 地图渲染 | L | 2-3周 | Phase 3B |
| Phase 3B | 服务端 — 地图 & 世界管理 | L | 2周 | Phase 3A |
| Phase 4A | 客户端 — 角色系统 | XL | 3-4周 | Phase 4B |
| Phase 4B | 服务端 — 游戏对象 & 玩家逻辑 | XL | 3-4周 | Phase 4A |
| Phase 5A | 客户端 — 网络层 & 输入 | L | 2周 | Phase 5B |
| Phase 5B | 服务端 — 消息处理 & 游戏循环 | L | 2-3周 | Phase 5A |
| Phase 6A | 客户端 — 战斗 & 魔法视觉 | L | 2-3周 | Phase 6B |
| Phase 6B | 服务端 — 怪物AI & 战斗系统 | XL | 3-4周 | Phase 6A |
| Phase 7A | 客户端 — UI系统 | L | 2-3周 | Phase 7B |
| Phase 7B | 服务端 — NPC & 脚本系统 | XL | 3-4周 | Phase 7A |
| Phase 8A | 客户端 — 打磨 & 音效 | M | 2-3周 | Phase 8B |
| Phase 8B | 服务端 — 进阶系统 & 优化 | L | 2-3周 | Phase 8A |

**总预计工作量**: 25-35周（6-9个月），单人开发（通过并行开发节省约30%时间）

---

## 关键设计决策

1. **internal/protocol/ 集中共享类型** — 客户端和服务端从同一包导入，保证线路兼容性。

2. **internal/engine/ 共享渲染** — 从mapviewer/wilviewer提取OpenGL样板代码到共享包。

3. **internal/game/ ECS 架构** — 使用组件化实体系统替代深度继承，更灵活、更易测试。

4. **internal/storage/ 数据层** — 统一封装 SQLite 操作，便于维护和测试。

5. **单端口统一监听** — server 监听一个端口（默认7000），内部自动路由登录/角色/游戏消息。

6. **每连接一个goroutine** — 每个客户端连接有自己的goroutine。通过channel向游戏引擎线程传递消息。

7. **单线程游戏循环** — 10 ticks/秒在单个goroutine中运行。避免游戏状态的并发修改。

8. **SQLite 数据存储** — 账号、角色、行会数据统一存储在 SQLite 数据库中。

9. **接口驱动设计** — 核心系统通过接口交互，便于单元测试和替换实现。

10. **Channel-based 消息总线** — 客户端和服务端内部使用 channel 传递消息，避免全局状态。

11. **客户端动作锁定** — 必须等待服务器确认后才能发送下一个动作。

12. **6Bit编码保真** — 精确重新实现Delphi编码，不做重新设计。

13. **不使用外部游戏引擎** — 自定义OpenGL渲染，保持依赖最小化。

---

## 验证方案

| 阶段 | 验证方式 | 集成里程碑 |
|------|----------|-----------|
| Phase 1 | 单元测试: 编码往返测试 | — |
| Phase 2A | 运行客户端，窗口打开 | — |
| Phase 2B | 启动服务端，监听端口 | — |
| Phase 3A | 渲染完整地图 | 里程碑3 |
| Phase 3B | 加载所有地图 | 里程碑3 |
| Phase 4A | 创建测试角色 | 里程碑4 |
| Phase 4B | 角色数据加载/保存 | 里程碑4 |
| Phase 5A | 连接测试服务器 | 里程碑5 |
| Phase 5B | 处理玩家连接 | 里程碑5 |
| Phase 6A | 战斗视觉效果 | 里程碑6 |
| Phase 6B | 怪物AI和战斗 | 里程碑6 |
| Phase 7A | UI窗口显示 | 里程碑7 |
| Phase 7B | NPC交互 | 里程碑7 |
| Phase 8A | 音效和性能 | 里程碑8 |
| Phase 8B | 全面测试 | 里程碑8 |

---

> **注**: 服务端配置文件分析和 Server Config Converter 工具设计已抽离为独立文档，详见服务端资源文件分析及转换.md。

